apiVersion: v1
kind: Service
metadata:
  name: cloudcad-app
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.app.port }}
      targetPort: {{ .Values.app.port }}
  selector: { app: cloudcad, component: app }
