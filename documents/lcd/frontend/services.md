# 前端服务与工具

**路径**: `packages/frontend/src/`

## 服务层

**路径**: `services/`

API 调用封装，与后端 API 对应。

## 状态管理

**路径**: `stores/`

全局状态管理。

## 上下文

**路径**: `contexts/`

React Context 定义。

## 类型定义

**路径**: `types/`

TypeScript 类型定义。

## 常量

**路径**: `constants/`

前端常量定义。

## 工具函数

**路径**: `utils/`

通用工具函数。

## 样式

**路径**: `styles/`

全局样式、CSS 变量定义。

## 配置

**路径**: `config/`

前端配置。
