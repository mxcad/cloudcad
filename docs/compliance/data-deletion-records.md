# 敏感数据删除操作记录

对应等保三级「剩余信息保护 / 数据删除与销毁」条款的操作证据留档。每次删除含密钥/敏感数据的文件时在此追加一条记录（时间 / 操作人 / 对象 / 原因 / 关联票）。

## 记录

| 时间 | 操作人 | 删除对象 | 原因 | 关联 |
|---|---|---|---|---|
| 2026-08-31 | wudexiong | `packages/backend/.env.bak`（本地文件，已被 .gitignore 覆盖，从未入 VCS） | 含历史密钥的备份残留，无保留价值；secrets 管理定案为「明文 .env + chmod 600 + gitignore 全覆盖 + gitleaks 兜底」，备份文件不在该组合内，须删除 | #424 / #413 |
