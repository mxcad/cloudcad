["\n# 前端架构审查报告\n\n> 审查日期：2026-05-08 | 审查范围：`packages/frontend/src` 所有源码\n\n## 严重程度定义\n\n| 级别 | 说明 |\n|------|------|\n| 🔴 严重 | 运行时风险（WebGL 泄漏、内存溢出、竞态条件、安全漏洞） |\n| 🟠 高 | 架构反模式（巨型组件、关注点混淆、跨模块耦合） |\n| 🟡 中 | 可维护性问题（代码重复、低内聚、缺少抽象） |\n| 🟢 低 | 改进建议（命名规范、小重构） |\n\n---\n\n## 1. 组件拆分与复用\n\n### ✅ 良好实践\n\n- **页面级组件拆分已起步**：`Login/`、`Register/`、`Profile/`、`UserManagement/` 等页面有独立的 `components/`、`hooks/` 子目录，业务逻辑已部分提取到 hook 中\n- **FileItem 共享组件**：1911 行的 `components/FileItem.tsx` 是核心共享组件，被 Dashboard、FileSystemManager 等多个页面复用\n- **通用 UI 组件库**：`components/ui/` 下有 Button、Modal、Toast、ConfirmDialog 等 Radix UI 封装组件\n\n### 🔴 问题 1：巨型组件 CADEditorDirect.tsx（1331 行）\n\n- **文件**：`src/pages/CADEditorDirect.tsx`\n- **行数**：1331 行\n- **严重程度**：🔴 严重\n- **问题描述**：\n  该文件承担了过多职责：路由解析 + CAD 引擎生命周期 + 文件加载 + 权限检查 + 主题同步 + 外部参照上传 + 另存为/下载格式弹窗 + 登录提示。所有业务逻辑都以 useEffect + useState 的形式内联在同一组件中，包括：\n  - 路由监听（第 408-436 行）\n  - 文件加载（第 482-756 行，274 行）\n  - 主页模式初始化（第 812-875 行）\n  - 权限检查（第 233-257 行）\n  - 主题同步（第 275-336 行）\n  - 保存/另存为事件监听（第 878-998 行）\n  - 文件打开事件监听（第 1024-1084 行）\n  - 公开文件上传事件监听（第 759-803 行）\n  - 新建文件事件监听（第 1087-1103 行）\n  - 下载格式处理（第 1180-1215 行）\n  以上 10 个独立职责的代码块混在一个组件中，导致：\n  1. 任何改动都需测试整个文件，变更风险高\n  2. 16 个 useState + 14 个 useRef + 10 个 useEffect，状态追踪极其困难\n  3. 每个 useEffect 的依赖数组都很复杂，容易出现遗漏\n  4. React Strict Mode 下双挂载会触发多次网络请求\n- **修复建议**：\n  1. 提取 `useCadFileLoader` hook — 封装文件加载逻辑（第 482-756 行）\n  2. 提取 `useCadPermissions` hook — 封装 CAD 权限检查（第 233-257 行）\n  3. 提取 `useCadThemeSync` hook — 封装 Vuetify 主题同步（第 275-336 行）\n  4. 提取 `useCadEventListeners` hook — 封装所有 CustomEvent 监听器\n  5. 提取 `useHomeModeInit` hook — 封装主页模式初始化逻辑\n  6. 提取 `useDownloadHandler` hook — 封装下载格式处理\n  7. CADEditorDirect 组件本身只做渲染布局，组合这些 hook\n- **是否需要用户确认**：否（纯提取重构，不改功能逻辑）\n  8. CADEditorDirect 组件本身只做渲染布局，组合这些 hook\n- **合并大文件小标题防止窗口中断**：\n\n---\n\n### 🟠 问题 2：Profile.tsx 巨型组件（1132 行）\n\n- **文件**：`src/pages/Profile.tsx`\n- **行数**：1132 行\n- **严重程度**：🟠 高\n- **问题描述**：\n  Profile 页面集成了 5 个 Tab 的全部表单逻辑：个人信息、密码修改、邮箱绑定、手机绑定、微信绑定、账户注销。密码表单、邮箱表单、手机表单的所有 handler 函数（handlePasswordSubmit、handleSendBindCode、handleVerifyOldPhone 等）都定义在同一文件中，每个 handler 包含 30-50 行的 try-catch 错误处理逻辑。共计约 20 个独立 handler 函数混在一起。\n- **修复建议**：\n  虽然 Profile 已将子 Tab 拆分为 `Profile/ProfilePasswordTab.tsx` 等独立组件，但主文件 Profile.tsx 仍然持有所有 handler 逻辑。建议：\n  1. 密码相关逻辑 → `usePasswordForm` hook（已部分实现，但仍大量在 Profile.tsx 内）\n  2. 邮箱绑定全流程 → `useEmailBindFlow` hook\n  3. 手机绑定全流程 → `usePhoneBindFlow` hook\n  4. 注销流程 → `useAccountDeactivate` hook（已有 `useAccountDeactivate` 部分实现）\n  5. Profile.tsx 减少到约 200-300 行，仅做 Tab 路由和状态编排\n- **是否需要用户确认**：否（功能重构，不影响用户可见行为）\n\n### 🟡 问题 3：超过 300 行组件清单\n\n文件行数统计：\n| 文件 | 行数 |\n|------|------|\n| `services/mxcadManager/index.ts` | 2367 |\n| `components/FileItem.tsx` | 1911 |\n| `pages/CADEditorDirect.tsx` | 1332 |\n| `pages/Login/index.tsx` | 836 |\n| `pages/FileSystemManager/index.tsx` | 783 |\n| `pages/RoleManagement.tsx` | 731 |\n| `pages/Dashboard.tsx` | 593 |\n| `pages/UserManagement/index.tsx` | 580 |\n| `components/file-system-manager/FileSystemContent.tsx` | 573 |\n| `pages/FileSystemManager/FileSystemContent.tsx` | 528 |\n| `components/sidebar/SidebarContainer.tsx` | 460 |\n\n其中有 8 个文件超过 500 行，4 个文件超过 1000 行。特别是 `mxcadManager/index.ts` 达到 2367 行（见问题 7）和 `FileItem.tsx` 达到 1911 行。\n\n- **严重程度**：🟡 中\n- **问题描述**：`FileItem.tsx`（1911 行）作为核心共享组件，包含了文件菜单、拖拽、缩略图、权限提示等多种职责。Login/index.tsx（836 行）混合了登录表单、验证逻辑、样式定义。\n- **修复建议**：\n  1. `FileItem.tsx`：提取右键菜单逻辑为 `useFileContextMenu` hook，提取拖拽为 `useFileDrag` hook\n  2. `Login/index.tsx`：表单验证逻辑已有 `useLoginForm` hook，进一步提取样式常量和事件处理\n  3. `FileSystemManager/FileSystemContent.tsx` 与 `components/file-system-manager/FileSystemContent.tsx` 存在两份内容相似的文件：是否需要确认哪个是真实使用的版本，删除冗余文件\n- **是否需要用户确认**：是（需确认 FileSystemContent.tsx 保留哪个）\n\n### 🟡 问题 4：FileSystemContent.tsx 重复\n\n- **文件**：\n  - `src/pages/FileSystemManager/FileSystemContent.tsx`（528 行）\n  - `src/components/file-system-manager/FileSystemContent.tsx`（573 行）\n- **严重程度**：🟡 中\n- **问题描述**：\n  存在两个同名组件分属 pages 和 components 目录。虽然 props 接口不同，但名称相同、职责高度重叠（文件列表渲染），存在开发时混淆风险。需要确认是否为开发中冗余版或 A/B 测试版。\n- **修复建议**：\n  1. 确认只保留一个版本\n  2. 如两个版本各有用途，改名为 `ProjectFileList` 和 `FileGridList` 以区分\n- **是否需要用户确认**：是（需要确认哪个是实际使用的版本）\n\n---\n\n## 2. 状态管理（Zustand Store）\n\n### ✅ 良好实践\n\n- **3 个 Store 职责清晰**：`fileSystemStore`（文件系统 UI 状态）、`notificationStore`（应用通知）、`uiStore`（全局 Toast/Modal/Loading）\n- **无跨 Store 循环依赖**：各 Store 通过 Create 独立创建，不互相引用\n- **fileSystemStore 使用了 persist 中间件**：合理地将 viewMode、sortBy、pageSize 等 UI 偏好持久化到 localStorage\n- **partialize 配置合理**：仅持久化 UI 偏好，不持久化 currentPath、selectedItems 等运行时状态\n\n### 🟡 问题 5：fileSystemStore 管理了不应持久化的数据\n\n- **文件**：`src/stores/fileSystemStore.ts:17-27`\n- **严重程度**：🟡 中\n- **问题描述**：\n  `personalSpaceId` 和 `personalSpaceIdLoading` 存储在 Zustand 全局 store 中。personalSpaceId 是通过 API 调用获取的用户私人空间 ID，属于请求级数据。将其放在全局 store 中意味着：\n  1. 其他任何组件都可以读写它，增加不必要的耦合\n  2. 被 persist middleware 排除在持久化外（目前确实排除了），但仍在全局命名空间\n  3. 如果用户退出登录→重新登录另一个账号，store 不会自动重置\n- **修复建议**：将 `personalSpaceId` 和 `personalSpaceIdLoading` 从全局 store 移至 React Context（如 AuthContext）或使用 React Query 的缓存\n- **是否需要用户确认**：否\n\n### 🟢 问题 6：notificationStore 和 uiStore.toasts 功能重叠\n\n- **文件**：\n  - `src/stores/notificationStore.ts:49-71`\n  - `src/stores/uiStore.ts:44-53`\n- **严重程度**：🟢 低\n- **问题描述**：\n  notificationStore 和 uiStore 都管理 Toast/Notification 消息。notificationStore 的 `addNotification` 和 uiStore 的 `addToast` 功能几乎相同，都是创建消息 + 自动移除。notificationStore 多了 `unreadCount` 和持久化能力，但两者的数据结构不同（Notification 有 title/message/action，Toast 只有 message）。\n- **修复建议**：统一为一种 Toast 系统。如果通知需要持久化和未读计数，则保留 notificationStore；否则简化为 uiStore.toasts\n- **是否需要用户确认**：是（涉及 UI 交互变更）\n\n### 🟢 问题 7：全局 loading 状态用途有限\n\n- **文件**：`src/stores/uiStore.ts:33-38`\n- **严重程度**：🟢 低\n- **问题描述**：\n  uiStore 定义了 `globalLoading`、`loadingMessage`、`loadingProgress` 三个字段，同时页面内各组件还有自己的 `loading` useState。全局 loading 状态和局部 loading 状态并存，但实际 UI 渲染中使用的是局部状态（如 CADEditorDirect 中的 `loading` useState）。\n- **修复建议**：如果全局 loading 没有被实际使用（如全局遮罩层），则移除相关字段\n- **是否需要用户确认**：否\n\n---\n\n## 3. 路由设计\n\n### ✅ 良好实践\n\n- **代码分割合理**：所有页面使用 `React.lazy()` 懒加载，CAD 编辑器单独分包\n- **路由保护完善**：`ProtectedRoute`（认证检查）+ `PermissionRoute`（权限检查）双层守卫\n- **Loading 状态处理**：ProtectedRoute 中的 `loading` 状态防止认证检查期间的闪跳\n- **CAD 编辑器路由守卫**：`CADEditorRouteGuard` 仅在 CAD 路由下挂载 CAD 引擎，避免不必要的重依赖加载\n- **URL 结构清晰**：`/projects/:projectId/files/:nodeId` 的嵌套结构清晰\n\n### 🟡 问题 8：路由守卫组件作为内联定义\n\n- **文件**：`src/App.tsx:77-124`\n- **严重程度**：🟡 中\n- **问题描述**：\n  `ProtectedRoute` 和 `PermissionRoute` 两个守卫组件直接在 App.tsx 中内联定义。虽然使用了 `React.memo`，但内联定义的函数组件会在每次 App 渲染时重新创建组件引用，可能导致不必要的重渲染。\n- **修复建议**：将 `ProtectedRoute` 和 `PermissionRoute` 移至独立的 `src/components/guards/` 目录\n- **是否需要用户确认**：否\n\n### 🟢 问题 9：重定向路由使用内联箭头函数\n\n- **文件**：`src/App.tsx:213,229,241,242,245,246,294`\n- **严重程度**：🟢 低\n- **问题描述**：\n  多处使用 `<Route path=\"/\" element={<Navigate to=\"/cad-editor\" replace />} />` 的模式做重定向。在嵌套 Routes 中出现重复定义（如 App 层定义 `/ -> /cad-editor`，ProtectedRoute 内部也定义一次）。\n- **修复建议**：统一在路由配置对象中定义重定向规则，避免嵌套 Routes 中的重复\n- **是否需要用户确认**：否\n\n---\n\n## 4. 关注点分离（Separation of Concerns）\n\n### ✅ 良好实践\n\n- **Dashboard 页面**：使用了 `useDashboardStats` 和 `useDashboardProjects` 两个数据 hook，业务逻辑与 UI 分离良好（593 行相对克制）\n- **FileSystemManager**：使用 `useFileSystem` 大 hook 封装了全部文件系统操作，页面组件只做布局编排\n- **Profile 页**：Tab 子组件已有独立的 hook（usePasswordChange、useEmailBind 等）\n- **Login 页**：表单逻辑提取到 `useLoginForm` hook 中\n\n### 🟡 问题 10：SidebarNodeList 直接发起 API 调用\n\n- **文件**：`src/components/sidebar/SidebarNodeList.tsx:9-10`\n- **严重程度**：🟡 中\n- **问题描述**：\n  侧边栏子组件直接从 `@/api-sdk` 导入 `fileSystemControllerGetNodeChildren` 和 `libraryControllerGetNodeChildren` 发起 API 调用，没有通过 service 层或 hook 封装。这导致：\n  1. 组件直接依赖 API SDK，测试时需要 mock API 模块\n  2. 数据获取与组件生命周期耦合，错误处理、loading 状态在组件内管理\n- **修复建议**：创建 `useSidebarNodes` hook，封装 API 调用、loading/error 状态\n- **是否需要用户确认**：否\n\n### 🔴 问题 11：CADEditorDirect 中存在大量直接 API 调用\n\n- **文件**：`src/pages/CADEditorDirect.tsx`\n- **行号**：第 18、211-223、244-246、518-519、574-575、1136-1137、1147-1148、1186-1188 行\n- **严重程度**：🔴 严重\n- **问题描述**：\n  CADEditorDirect 直接导入并使用 10+ 个 API SDK 函数：\n  - `fileSystemControllerGetNode`\n  - `fileSystemControllerGetRootNode`\n  - `fileSystemControllerDownloadNodeWithFormat`\n  - `fileSystemControllerGetPersonalSpace`\n  - `fileSystemControllerCheckProjectPermission`\n  - `libraryControllerGetDrawingNode`\n  - `libraryControllerGetBlockNode`\n  这些 API 调用直接写在 useEffect 内部，导致组件与 API 层强耦合，无法单独测试\n- **修复建议**：提取文件加载逻辑到 `useCadFileService` hook，将所有 API 调用集中管理\n- **是否需要用户确认**：否\n\n### 🟡 问题 12：FileSystemManager/index.tsx 中直接引用 API SDK\n\n- **文件**：`src/pages/FileSystemManager/index.tsx:26-29`\n- **严重程度**：🟡 中\n- **问题描述**：\n  FileSystemManager 顶部直接导入 `fileSystemControllerUpdateNode`、`fileSystemControllerCreateProject`、`fileSystemControllerGetPersonalSpace` 等 API 函数。虽然 `useFileSystem` hook 封装了大部分操作，但还有残留的直接 API 引用用于提交项目和获取个人空间。\n- **修复建议**：\n  1. 项目创建/更新 → 移到 `useProjectManagement` hook 内部\n  2. 获取私人空间 → 移到 `useFileSystem` hook 内部\n- **是否需要用户确认**：否\n\n### 🟢 问题 13：services 目录下无独立 API Service 文件\n\n- **文件**：`src/services/` 目录\n- **严重程度**：🟢 低\n- **问题描述**：\n  `src/services/` 目录下只有 `mxcadManager/` 一个模块。API 调用通过 `@/api-sdk`（自动生成的 axios 客户端）直接访问，没有独立的 service 封装层（如 `src/services/fileSystemService.ts`）。虽然 api-sdk 提供了类型安全，但缺少业务语义层（如 `uploadFileAndWaitConvert()` 组合操作）。\n- **修复建议**：创建 `src/services/fileSystemService.ts` 封装多步骤业务操作\n- **是否需要用户确认**：否\n\n---\n\n## 5. API 类型使用\n\n### ✅ 良好实践\n\n- **API 类型自动生成**：`src/types/api-client.ts` 通过 openapicmd 从 Swagger JSON 自动生成\n- **api-sdk 封装**：`src/api-sdk/` 基于生成的类型提供 axios 客户端函数\n\n### 🟡 问题 14：CADEditorDirect 中使用内联 `as` 类型断言绕过生成类型\n\n- **文件**：`src/pages/CADEditorDirect.tsx`\n- **行号**：第 512、515、519、1136、1147 行\n- **严重程度**：🟡 中\n- **问题描述**：\n  多处使用 `as unknown as { data: ... }` 双重类型断言来绕过 api-sdk 生成的类型。例如：\n  ```typescript\n  const { data: fileNode } = await fileSystemControllerGetNode({...})\n    as unknown as { data: unknown };\n  file = fileNode as typeof file;\n  ```\n  这说明 api-sdk 生成的返回类型与实际返回值不匹配（或者开发者没有找到正确的类型），导致放弃类型安全。\n- **修复建议**：\n  1. 检查 api-sdk 是否正确生成了 `fileSystemControllerGetNode` 的返回类型\n  2. 如果生成类型不正确，修复 Swagger JSON 或 openapicmd 配置\n  3. 使用 api-client.ts 中定义的正确类型，而非内联 `typeof file`\n- **是否需要用户确认**：否\n\n### 🟡 问题 15：Dashboard 中使用 `as FileSystemNode` 类型转换\n\n- **文件**：`src/pages/Dashboard.tsx:506-518`\n- **严重程度**：🟡 中\n- **问题描述**：\n  Dashboard 在渲染项目列表时手动构造 `FileSystemNode` 对象并通过 `as` 断言：\n  ```typescript\n  node={ {\n    id: project.id,\n    name: project.name,\n    isFolder: true,\n    ...\n  } as FileSystemNode }\n  ```\n  这说明项目数据返回结构与 FileSystemNode 不兼容，用 `as` 强制类型\n- **修复建议**：使用 `toFileSystemNode()` 工具函数统一转换\n- **是否需要用户确认**：否\n\n### 🟢 问题 16：mxcadManager 中大量使用 any 类型\n\n- **文件**：`src/services/mxcadManager/index.ts`\n- **严重程度**：🟢 低\n- **问题描述**：\n  由于与 mxcad-app SDK 的交互涉及 Vue 对象（如 `vuetify.theme.global.name.value`），部分代码使用了隐式 any。这是与第三方库集成的固有问题，影响可控。\n- **修复建议**：为 mxcad-app 关键接口声明 `.d.ts` 补充类型\n- **是否需要用户确认**：否\n\n---\n\n## 6. CAD 编辑器集成\n\n### ✅ 良好实践\n\n- **单例模式正确**：`mxcadManager` 作为模块级单例导出，避免多个 CAD 实例\n- **WebGL 保护方案**：CADEditorDirect 使用 `visibility: hidden/visible + z-index` 控制显示而非卸载，保护 WebGL 上下文\n- **竞态条件处理**：`pendingShowActionRef` 解决了 hideEditor 和 showMxCAD 之间的竞态\n- **Strict Mode 兼容**：`homeInitStartedRef` 使用组件级 ref（非 window 全局属性）防止双挂载导致的永久 loading\n- **缓存时间戳机制**：使用 `updatedAt` 字段构造 `?t=` 参数确保获取最新文件\n- **模块拆分合理**：mxcadManager 拆分为 mxcadTypes、mxcadSave、mxcadThumbnail、mxcadExtRef、mxcadCheck 子模块\n\n### 🔴 问题 17：mxcadManager/index.ts 模块级副作用代码\n\n- **文件**：`src/services/mxcadManager/index.ts:157`\n- **严重程度**：🟠 高（分类修正：非严重级别）\n- **行号**：第 157 行\n- **问题描述**：\n  `setupBeforeUnloadHandler()` 在模块加载时立即执行：\n  ```typescript\n  // 在模块加载时自动启用浏览器关闭保护\n  setupBeforeUnloadHandler();\n  ```\n  这意味着只要 import 了 mxcadManager 的任何导出函数，就会注册 beforeunload 事件。而 mxcadManager 被 CADEditorDirect 的 `loadMxCADDependencies()` 动态 import，也意味着 beforeunload handler 只在首次打开 CAD 编辑器后才注册。但如果用户在未修改文档的情况下触发 beforeunload，handler 仍会执行。\n- **修复建议**：将 `setupBeforeUnloadHandler()` 改为显式调用（如在 `initializeMxCADView()` 中调用），而非模块级副作用\n- **是否需要用户确认**：否\n\n### 🟡 问题 18：mxcadManager 使用模块级变量管理全局状态\n\n- **文件**：`src/services/mxcadManager/index.ts:94-108`\n- **严重程度**：🟡 中\n- **问题描述**：\n  mxcadManager 使用模块级 `let` 变量管理全局状态：\n  ```typescript\n  let currentFileInfo = null;\n  let cachedPersonalSpaceId = null;\n  let documentModified = false;\n  let pendingImages: PendingImage[] = [];\n  let currentMxwebUrl: string | null = null;\n  let navigateFunction: ((path: string) => void) | null = null;\n  ```\n  React 组件通过 `setCurrentFileInfo()` 等 setter 函数修改这些变量。这不是 Zustand store，也不是 React Context，而是一个独立的全局可变状态。问题：\n  1. 状态修改不会触发 React 重渲染，依赖 CustomEvent 手动同步\n  2. 多组件实例场景下状态共享不可预测\n  3. React Strict Mode 下模块级变量不会在 unmount 时重置\n- **修复建议**：\n  1. 将 mxcadManager 的状态迁移到 Zustand store（如 `useCadStore`）\n  2. 或在 mxcadManager 内部使用发布-订阅模式触发状态更新\n- **是否需要用户确认**：否\n\n### 🟡 问题 19：mxcadManager/index.ts 巨型文件（2367 行）\n\n- **文件**：`src/services/mxcadManager/index.ts`\n- **行数**：2367 行\n- **严重程度**：🟡 中\n- **问题描述**：\n  虽然有子模块拆分（mxcadSave、mxcadThumbnail 等），但 index.ts 仍然有 2367 行，包含了：\n  - 导航逻辑（NAVIGATION_PATHS + calculateReturnPath 等）\n  - 文件上传逻辑\n  - 命令处理（返回命令、保存命令、打开文件命令等）\n  - DOM 对话框（showUnsavedChangesDialog 用 innerHTML 创建）\n  - 主题同步逻辑\n  - 浏览器关闭保护\n- **修复建议**：\n  1. 提取 `mxcadNavigation.ts` — 导航路径和返回计算\n  2. 提取 `mxcadCommands.ts` — 命令注册和处理\n  3. 提取 `mxcadDialogs.ts` — DOM 对话框\n  4. 提取 `mxcadFileUpload.ts` — 文件上传逻辑\n  5. index.ts 只保留核心的 MxCADManager 类和重新导出\n- **是否需要用户确认**：否\n\n### 🟡 问题 20：showUnsavedChangesDialog 使用 innerHTML 构造 DOM\n\n- **文件**：`src/services/mxcadManager/index.ts:166-332`\n- **行号**：第 166-332 行\n- **严重程度**：🟡 中\n- **问题描述**：\n  未保存更改确认对话框通过纯 DOM 操作（innerHTML + addEventListener）创建，不经过 React。虽然功能可用，但：\n  1. 样式通过内联 `style` 字符串维护，无法享受 Tailwind CSS 和设计系统\n  2. 不受 React 生命周期管理，可能导致内存泄漏（事件监听器清理依赖 setTimeout）\n  3. 不符合项目中其他 Modal 使用 Radix UI 的统一模式\n- **修复建议**：使用 React Portal 渲染对话框组件，保持与其他 Modal 一致的模式\n- **是否需要用户确认**：否\n\n---\n\n## 总结\n\n| # | 类别 | 问题 | 文件 | 行号 | 严重程度 | 需确认 |\n|---|------|------|------|------|----------|--------|\n| 1 | 组件拆分 | CADEditorDirect 巨型组件 (1331行) | CADEditorDirect.tsx | 1-1331 | 🔴 严重 | 否 |\n| 2 | 组件拆分 | Profile 巨型组件 (1132行) | Profile.tsx | 1-1132 | 🟠 高 | 否 |\n| 3 | 组件拆分 | 超 300 行组件清单 (8 个超 500 行) | 多个文件 | — | 🟡 中 | 部分 |\n| 4 | 组件拆分 | FileSystemContent.tsx 重复 | 两个文件 | — | 🟡 中 | 是 |\n| 5 | 状态管理 | personalSpaceId 不应在全局 store | fileSystemStore.ts | 17-27 | 🟡 中 | 否 |\n| 6 | 状态管理 | notificationStore 与 uiStore.toasts 重叠 | 两个 store | — | 🟢 低 | 是 |\n| 7 | 状态管理 | 全局 loading 状态未使用 | uiStore.ts | 33-38 | 🟢 低 | 否 |\n| 8 | 路由设计 | 路由守卫内联定义 | App.tsx | 77-124 | 🟡 中 | 否 |\n| 9 | 路由设计 | 重定向路由嵌套重复 | App.tsx | 多处 | 🟢 低 | 否 |\n| 10 | 关注点分离 | SidebarNodeList 直接调用 API | SidebarNodeList.tsx | 9-10 | 🟡 中 | 否 |\n| 11 | 关注点分离 | CADEditorDirect 中 10+ 处直接 API 调用 | CADEditorDirect.tsx | 多处 | 🔴 严重 | 否 |\n| 12 | 关注点分离 | FileSystemManager 残留直接 API 引用 | FileSystemManager/index.tsx | 26-29 | 🟡 中 | 否 |\n| 13 | 关注点分离 | 缺少 Service 封装层 | services/ 目录 | — | 🟢 低 | 否 |\n| 14 | API 类型 | 内联 `as unknown as` 绕过生成类型 | CADEditorDirect.tsx | 512-1147 | 🟡 中 | 否 |\n| 15 | API 类型 | Dashboard 中使用 `as FileSystemNode` 断言 | Dashboard.tsx | 506-518 | 🟡 中 | 否 |\n| 16 | API 类型 | mxcadManager 大量 any 类型 | mxcadManager/index.ts | — | 🟢 低 | 否 |\n| 17 | CAD 集成 | 模块级副作用注册 beforeunload | mxcadManager/index.ts | 157 | 🟠 高 | 否 |\n| 18 | CAD 集成 | 模块级 let 变量管理全局状态 | mxcadManager/index.ts | 94-108 | 🟡 中 | 否 |\n| 19 | CAD 集成 | mxcadManager/index.ts 巨型文件 (2367行) | mxcadManager/index.ts | 1-2367 | 🟡 中 | 否 |\n| 20 | CAD 集成 | innerHTML 构造对话框 | mxcadManager/index.ts | 166-332 | 🟡 中 | 否 |\n\n**统计**：🔴 2 严重 · 🟠 2 高 · 🟡 12 中 · 🟢 4 低 | 需用户确认 3 项\n\n### 优先修复路线\n\n| 优先级 | 问题编号 | 原因 |\n|--------|----------|------|\n| P0 | #1, #11 | CADEditorDirect 巨型组件 + 直接 API 调用，影响可维护性和测试性 |\n| P1 | #17 | 模块级副作用可能引入隐蔽的 bug |\n| P2 | #5, #18 | 全局状态管理混乱 |\n| P3 | #2, #19 | 巨型组件和文件的拆分（长期改进） |\n| P4 | #3, #4, #6, #14, #15, #20 | 代码质量和一致性改进 |\n","\n# 前端架构审查报告\n\n> 审查日期：2026-05-08 | 审查范围：`packages/frontend/src` 所有源码\n\n## 严重程度定义\n\n| 级别 | 说明 |\n|------|------|\n| 🔴 严重 | 运行时风险（WebGL 泄漏、内存溢出、竞态条件、安全漏洞） |\n| 🟠 高 | 架构反模式（巨型组件、关注点混淆、跨模块耦合） |\n| 🟡 中 | 可维护性问题（代码重复、低内聚、缺少抽象） |\n| 🟢 低 | 改进建议（命名规范、小重构） |\n\n---\n\n## 1. 组件拆分与复用\n\n### ✅ 良好实践\n\n- **页面级组件拆分已起步**：`Login/`、`Register/`、`Profile/`、`UserManagement/` 等页面有独立的 `components/`、`hooks/` 子目录，业务逻辑已部分提取到 hook 中\n- **FileItem 共享组件**：1911 行的 `components/FileItem.tsx` 是核心共享组件，被 Dashboard、FileSystemManager 等多个页面复用\n- **通用 UI 组件库**：`components/ui/` 下有 Button、Modal、Toast、ConfirmDialog 等 Radix UI 封装组件\n\n### 🔴 问题 1：巨型组件 CADEditorDirect.tsx（1331 行）\n\n- **文件**：`src/pages/CADEditorDirect.tsx`\n- **行数**：1331 行\n- **严重程度**：🔴 严重\n- **问题描述**：\n  该文件承担了过多职责：路由解析 + CAD 引擎生命周期 + 文件加载 + 权限检查 + 主题同步 + 外部参照上传 + 另存为/下载格式弹窗 + 登录提示。所有业务逻辑都以 useEffect + useState 的形式内联在同一组件中，包括：\n  - 路由监听（第 408-436 行）\n  - 文件加载（第 482-756 行，274 行）\n  - 主页模式初始化（第 812-875 行）\n  - 权限检查（第 233-257 行）\n  - 主题同步（第 275-336 行）\n  - 保存/另存为事件监听（第 878-998 行）\n  - 文件打开事件监听（第 1024-1084 行）\n  - 公开文件上传事件监听（第 759-803 行）\n  - 新建文件事件监听（第 1087-1103 行）\n  - 下载格式处理（第 1180-1215 行）\n  以上 10 个独立职责的代码块混在一个组件中，导致：\n  1. 任何改动都需测试整个文件，变更风险高\n  2. 16 个 useState + 14 个 useRef + 10 个 useEffect，状态追踪极其困难\n  3. 每个 useEffect 的依赖数组都很复杂，容易出现遗漏\n  4. React Strict Mode 下双挂载会触发多次网络请求\n- **修复建议**：\n  1. 提取 `useCadFileLoader` hook — 封装文件加载逻辑（第 482-756 行）\n  2. 提取 `useCadPermissions` hook — 封装 CAD 权限检查（第 233-257 行）\n  3. 提取 `useCadThemeSync` hook — 封装 Vuetify 主题同步（第 275-336 行）\n  4. 提取 `useCadEventListeners` hook — 封装所有 CustomEvent 监听器\n  5. 提取 `useHomeModeInit` hook — 封装主页模式初始化逻辑\n  6. 提取 `useDownloadHandler` hook — 封装下载格式处理\n  7. CADEditorDirect 组件本身只做渲染布局，组合这些 hook\n- **是否需要用户确认**：否（纯提取重构，不改功能逻辑）\n  8. CADEditorDirect 组件本身只做渲染布局，组合这些 hook\n- **合并大文件小标题防止窗口中断**：\n\n---\n\n### 🟠 问题 2：Profile.tsx 巨型组件（1132 行）\n\n- **文件**：`src/pages/Profile.tsx`\n- **行数**：1132 行\n- **严重程度**：🟠 高\n- **问题描述**：\n  Profile 页面集成了 5 个 Tab 的全部表单逻辑：个人信息、密码修改、邮箱绑定、手机绑定、微信绑定、账户注销。密码表单、邮箱表单、手机表单的所有 handler 函数（handlePasswordSubmit、handleSendBindCode、handleVerifyOldPhone 等）都定义在同一文件中，每个 handler 包含 30-50 行的 try-catch 错误处理逻辑。共计约 20 个独立 handler 函数混在一起。\n- **修复建议**：\n  虽然 Profile 已将子 Tab 拆分为 `Profile/ProfilePasswordTab.tsx` 等独立组件，但主文件 Profile.tsx 仍然持有所有 handler 逻辑。建议：\n  1. 密码相关逻辑 → `usePasswordForm` hook（已部分实现，但仍大量在 Profile.tsx 内）\n  2. 邮箱绑定全流程 → `useEmailBindFlow` hook\n  3. 手机绑定全流程 → `usePhoneBindFlow` hook\n  4. 注销流程 → `useAccountDeactivate` hook（已有 `useAccountDeactivate` 部分实现）\n  5. Profile.tsx 减少到约 200-300 行，仅做 Tab 路由和状态编排\n- **是否需要用户确认**：否（功能重构，不影响用户可见行为）\n\n### 🟡 问题 3：超过 300 行组件清单\n\n文件行数统计：\n| 文件 | 行数 |\n|------|------|\n| `services/mxcadManager/index.ts` | 2367 |\n| `components/FileItem.tsx` | 1911 |\n| `pages/CADEditorDirect.tsx` | 1332 |\n| `pages/Login/index.tsx` | 836 |\n| `pages/FileSystemManager/index.tsx` | 783 |\n| `pages/RoleManagement.tsx` | 731 |\n| `pages/Dashboard.tsx` | 593 |\n| `pages/UserManagement/index.tsx` | 580 |\n| `components/file-system-manager/FileSystemContent.tsx` | 573 |\n| `pages/FileSystemManager/FileSystemContent.tsx` | 528 |\n| `components/sidebar/SidebarContainer.tsx` | 460 |\n\n其中有 8 个文件超过 500 行，4 个文件超过 1000 行。特别是 `mxcadManager/index.ts` 达到 2367 行（见问题 7）和 `FileItem.tsx` 达到 1911 行。\n\n- **严重程度**：🟡 中\n- **问题描述**：`FileItem.tsx`（1911 行）作为核心共享组件，包含了文件菜单、拖拽、缩略图、权限提示等多种职责。Login/index.tsx（836 行）混合了登录表单、验证逻辑、样式定义。\n- **修复建议**：\n  1. `FileItem.tsx`：提取右键菜单逻辑为 `useFileContextMenu` hook，提取拖拽为 `useFileDrag` hook\n  2. `Login/index.tsx`：表单验证逻辑已有 `useLoginForm` hook，进一步提取样式常量和事件处理\n  3. `FileSystemManager/FileSystemContent.tsx` 与 `components/file-system-manager/FileSystemContent.tsx` 存在两份内容相似的文件：是否需要确认哪个是真实使用的版本，删除冗余文件\n- **是否需要用户确认**：是（需确认 FileSystemContent.tsx 保留哪个）\n\n### 🟡 问题 4：FileSystemContent.tsx 重复\n\n- **文件**：\n  - `src/pages/FileSystemManager/FileSystemContent.tsx`（528 行）\n  - `src/components/file-system-manager/FileSystemContent.tsx`（573 行）\n- **严重程度**：🟡 中\n- **问题描述**：\n  存在两个同名组件分属 pages 和 components 目录。虽然 props 接口不同，但名称相同、职责高度重叠（文件列表渲染），存在开发时混淆风险。需要确认是否为开发中冗余版或 A/B 测试版。\n- **修复建议**：\n  1. 确认只保留一个版本\n  2. 如两个版本各有用途，改名为 `ProjectFileList` 和 `FileGridList` 以区分\n- **是否需要用户确认**：是（需要确认哪个是实际使用的版本）\n\n---\n\n## 2. 状态管理（Zustand Store）\n\n### ✅ 良好实践\n\n- **3 个 Store 职责清晰**：`fileSystemStore`（文件系统 UI 状态）、`notificationStore`（应用通知）、`uiStore`（全局 Toast/Modal/Loading）\n- **无跨 Store 循环依赖**：各 Store 通过 Create 独立创建，不互相引用\n- **fileSystemStore 使用了 persist 中间件**：合理地将 viewMode、sortBy、pageSize 等 UI 偏好持久化到 localStorage\n- **partialize 配置合理**：仅持久化 UI 偏好，不持久化 currentPath、selectedItems 等运行时状态\n\n### 🟡 问题 5：fileSystemStore 管理了不应持久化的数据\n\n- **文件**：`src/stores/fileSystemStore.ts:17-27`\n- **严重程度**：🟡 中\n- **问题描述**：\n  `personalSpaceId` 和 `personalSpaceIdLoading` 存储在 Zustand 全局 store 中。personalSpaceId 是通过 API 调用获取的用户私人空间 ID，属于请求级数据。将其放在全局 store 中意味着：\n  1. 其他任何组件都可以读写它，增加不必要的耦合\n  2. 被 persist middleware 排除在持久化外（目前确实排除了），但仍在全局命名空间\n  3. 如果用户退出登录→重新登录另一个账号，store 不会自动重置\n- **修复建议**：将 `personalSpaceId` 和 `personalSpaceIdLoading` 从全局 store 移至 React Context（如 AuthContext）或使用 React Query 的缓存\n- **是否需要用户确认**：否\n\n### 🟢 问题 6：notificationStore 和 uiStore.toasts 功能重叠\n\n- **文件**：\n  - `src/stores/notificationStore.ts:49-71`\n  - `src/stores/uiStore.ts:44-53`\n- **严重程度**：🟢 低\n- **问题描述**：\n  notificationStore 和 uiStore 都管理 Toast/Notification 消息。notificationStore 的 `addNotification` 和 uiStore 的 `addToast` 功能几乎相同，都是创建消息 + 自动移除。notificationStore 多了 `unreadCount` 和持久化能力，但两者的数据结构不同（Notification 有 title/message/action，Toast 只有 message）。\n- **修复建议**：统一为一种 Toast 系统。如果通知需要持久化和未读计数，则保留 notificationStore；否则简化为 uiStore.toasts\n- **是否需要用户确认**：是（涉及 UI 交互变更）\n\n### 🟢 问题 7：全局 loading 状态用途有限\n\n- **文件**：`src/stores/uiStore.ts:33-38`\n- **严重程度**：🟢 低\n- **问题描述**：\n  uiStore 定义了 `globalLoading`、`loadingMessage`、`loadingProgress` 三个字段，同时页面内各组件还有自己的 `loading` useState。全局 loading 状态和局部 loading 状态并存，但实际 UI 渲染中使用的是局部状态（如 CADEditorDirect 中的 `loading` useState）。\n- **修复建议**：如果全局 loading 没有被实际使用（如全局遮罩层），则移除相关字段\n- **是否需要用户确认**：否\n\n---\n\n## 3. 路由设计\n\n### ✅ 良好实践\n\n- **代码分割合理**：所有页面使用 `React.lazy()` 懒加载，CAD 编辑器单独分包\n- **路由保护完善**：`ProtectedRoute`（认证检查）+ `PermissionRoute`（权限检查）双层守卫\n- **Loading 状态处理**：ProtectedRoute 中的 `loading` 状态防止认证检查期间的闪跳\n- **CAD 编辑器路由守卫**：`CADEditorRouteGuard` 仅在 CAD 路由下挂载 CAD 引擎，避免不必要的重依赖加载\n- **URL 结构清晰**：`/projects/:projectId/files/:nodeId` 的嵌套结构清晰\n\n### 🟡 问题 8：路由守卫组件作为内联定义\n\n- **文件**：`src/App.tsx:77-124`\n- **严重程度**：🟡 中\n- **问题描述**：\n  `ProtectedRoute` 和 `PermissionRoute` 两个守卫组件直接在 App.tsx 中内联定义。虽然使用了 `React.memo`，但内联定义的函数组件会在每次 App 渲染时重新创建组件引用，可能导致不必要的重渲染。\n- **修复建议**：将 `ProtectedRoute` 和 `PermissionRoute` 移至独立的 `src/components/guards/` 目录\n- **是否需要用户确认**：否\n\n### 🟢 问题 9：重定向路由使用内联箭头函数\n\n- **文件**：`src/App.tsx:213,229,241,242,245,246,294`\n- **严重程度**：🟢 低\n- **问题描述**：\n  多处使用 `<Route path=\"/\" element={<Navigate to=\"/cad-editor\" replace />} />` 的模式做重定向。在嵌套 Routes 中出现重复定义（如 App 层定义 `/ -> /cad-editor`，ProtectedRoute 内部也定义一次）。\n- **修复建议**：统一在路由配置对象中定义重定向规则，避免嵌套 Routes 中的重复\n- **是否需要用户确认**：否\n\n---\n\n## 4. 关注点分离（Separation of Concerns）\n\n### ✅ 良好实践\n\n- **Dashboard 页面**：使用了 `useDashboardStats` 和 `useDashboardProjects` 两个数据 hook，业务逻辑与 UI 分离良好（593 行相对克制）\n- **FileSystemManager**：使用 `useFileSystem` 大 hook 封装了全部文件系统操作，页面组件只做布局编排\n- **Profile 页**：Tab 子组件已有独立的 hook（usePasswordChange、useEmailBind 等）\n- **Login 页**：表单逻辑提取到 `useLoginForm` hook 中\n\n### 🟡 问题 10：SidebarNodeList 直接发起 API 调用\n\n- **文件**：`src/components/sidebar/SidebarNodeList.tsx:9-10`\n- **严重程度**：🟡 中\n- **问题描述**：\n  侧边栏子组件直接从 `@/api-sdk` 导入 `fileSystemControllerGetNodeChildren` 和 `libraryControllerGetNodeChildren` 发起 API 调用，没有通过 service 层或 hook 封装。这导致：\n  1. 组件直接依赖 API SDK，测试时需要 mock API 模块\n  2. 数据获取与组件生命周期耦合，错误处理、loading 状态在组件内管理\n- **修复建议**：创建 `useSidebarNodes` hook，封装 API 调用、loading/error 状态\n- **是否需要用户确认**：否\n\n### 🔴 问题 11：CADEditorDirect 中存在大量直接 API 调用\n\n- **文件**：`src/pages/CADEditorDirect.tsx`\n- **行号**：第 18、211-223、244-246、518-519、574-575、1136-1137、1147-1148、1186-1188 行\n- **严重程度**：🔴 严重\n- **问题描述**：\n  CADEditorDirect 直接导入并使用 10+ 个 API SDK 函数：\n  - `fileSystemControllerGetNode`\n  - `fileSystemControllerGetRootNode`\n  - `fileSystemControllerDownloadNodeWithFormat`\n  - `fileSystemControllerGetPersonalSpace`\n  - `fileSystemControllerCheckProjectPermission`\n  - `libraryControllerGetDrawingNode`\n  - `libraryControllerGetBlockNode`\n  这些 API 调用直接写在 useEffect 内部，导致组件与 API 层强耦合，无法单独测试\n- **修复建议**：提取文件加载逻辑到 `useCadFileService` hook，将所有 API 调用集中管理\n- **是否需要用户确认**：否\n\n### 🟡 问题 12：FileSystemManager/index.tsx 中直接引用 API SDK\n\n- **文件**：`src/pages/FileSystemManager/index.tsx:26-29`\n- **严重程度**：🟡 中\n- **问题描述**：\n  FileSystemManager 顶部直接导入 `fileSystemControllerUpdateNode`、`fileSystemControllerCreateProject`、`fileSystemControllerGetPersonalSpace` 等 API 函数。虽然 `useFileSystem` hook 封装了大部分操作，但还有残留的直接 API 引用用于提交项目和获取个人空间。\n- **修复建议**：\n  1. 项目创建/更新 → 移到 `useProjectManagement` hook 内部\n  2. 获取私人空间 → 移到 `useFileSystem` hook 内部\n- **是否需要用户确认**：否\n\n### 🟢 问题 13：services 目录下无独立 API Service 文件\n\n- **文件**：`src/services/` 目录\n- **严重程度**：🟢 低\n- **问题描述**：\n  `src/services/` 目录下只有 `mxcadManager/` 一个模块。API 调用通过 `@/api-sdk`（自动生成的 axios 客户端）直接访问，没有独立的 service 封装层（如 `src/services/fileSystemService.ts`）。虽然 api-sdk 提供了类型安全，但缺少业务语义层（如 `uploadFileAndWaitConvert()` 组合操作）。\n- **修复建议**：创建 `src/services/fileSystemService.ts` 封装多步骤业务操作\n- **是否需要用户确认**：否\n\n---\n\n## 5. API 类型使用\n\n### ✅ 良好实践\n\n- **API 类型自动生成**：`src/types/api-client.ts` 通过 openapicmd 从 Swagger JSON 自动生成\n- **api-sdk 封装**：`src/api-sdk/` 基于生成的类型提供 axios 客户端函数\n\n### 🟡 问题 14：CADEditorDirect 中使用内联 `as` 类型断言绕过生成类型\n\n- **文件**：`src/pages/CADEditorDirect.tsx`\n- **行号**：第 512、515、519、1136、1147 行\n- **严重程度**：🟡 中\n- **问题描述**：\n  多处使用 `as unknown as { data: ... }` 双重类型断言来绕过 api-sdk 生成的类型。例如：\n  ```typescript\n  const { data: fileNode } = await fileSystemControllerGetNode({...})\n    as unknown as { data: unknown };\n  file = fileNode as typeof file;\n  ```\n  这说明 api-sdk 生成的返回类型与实际返回值不匹配（或者开发者没有找到正确的类型），导致放弃类型安全。\n- **修复建议**：\n  1. 检查 api-sdk 是否正确生成了 `fileSystemControllerGetNode` 的返回类型\n  2. 如果生成类型不正确，修复 Swagger JSON 或 openapicmd 配置\n  3. 使用 api-client.ts 中定义的正确类型，而非内联 `typeof file`\n- **是否需要用户确认**：否\n\n### 🟡 问题 15：Dashboard 中使用 `as FileSystemNode` 类型转换\n\n- **文件**：`src/pages/Dashboard.tsx:506-518`\n- **严重程度**：🟡 中\n- **问题描述**：\n  Dashboard 在渲染项目列表时手动构造 `FileSystemNode` 对象并通过 `as` 断言：\n  ```typescript\n  node={ {\n    id: project.id,\n    name: project.name,\n    isFolder: true,\n    ...\n  } as FileSystemNode }\n  ```\n  这说明项目数据返回结构与 FileSystemNode 不兼容，用 `as` 强制类型\n- **修复建议**：使用 `toFileSystemNode()` 工具函数统一转换\n- **是否需要用户确认**：否\n\n### 🟢 问题 16：mxcadManager 中大量使用 any 类型\n\n- **文件**：`src/services/mxcadManager/index.ts`\n- **严重程度**：🟢 低\n- **问题描述**：\n  由于与 mxcad-app SDK 的交互涉及 Vue 对象（如 `vuetify.theme.global.name.value`），部分代码使用了隐式 any。这是与第三方库集成的固有问题，影响可控。\n- **修复建议**：为 mxcad-app 关键接口声明 `.d.ts` 补充类型\n- **是否需要用户确认**：否\n\n---\n\n## 6. CAD 编辑器集成\n\n### ✅ 良好实践\n\n- **单例模式正确**：`mxcadManager` 作为模块级单例导出，避免多个 CAD 实例\n- **WebGL 保护方案**：CADEditorDirect 使用 `visibility: hidden/visible + z-index` 控制显示而非卸载，保护 WebGL 上下文\n- **竞态条件处理**：`pendingShowActionRef` 解决了 hideEditor 和 showMxCAD 之间的竞态\n- **Strict Mode 兼容**：`homeInitStartedRef` 使用组件级 ref（非 window 全局属性）防止双挂载导致的永久 loading\n- **缓存时间戳机制**：使用 `updatedAt` 字段构造 `?t=` 参数确保获取最新文件\n- **模块拆分合理**：mxcadManager 拆分为 mxcadTypes、mxcadSave、mxcadThumbnail、mxcadExtRef、mxcadCheck 子模块\n\n### 🔴 问题 17：mxcadManager/index.ts 模块级副作用代码\n\n- **文件**：`src/services/mxcadManager/index.ts:157`\n- **严重程度**：🟠 高（分类修正：非严重级别）\n- **行号**：第 157 行\n- **问题描述**：\n  `setupBeforeUnloadHandler()` 在模块加载时立即执行：\n  ```typescript\n  // 在模块加载时自动启用浏览器关闭保护\n  setupBeforeUnloadHandler();\n  ```\n  这意味着只要 import 了 mxcadManager 的任何导出函数，就会注册 beforeunload 事件。而 mxcadManager 被 CADEditorDirect 的 `loadMxCADDependencies()` 动态 import，也意味着 beforeunload handler 只在首次打开 CAD 编辑器后才注册。但如果用户在未修改文档的情况下触发 beforeunload，handler 仍会执行。\n- **修复建议**：将 `setupBeforeUnloadHandler()` 改为显式调用（如在 `initializeMxCADView()` 中调用），而非模块级副作用\n- **是否需要用户确认**：否\n\n### 🟡 问题 18：mxcadManager 使用模块级变量管理全局状态\n\n- **文件**：`src/services/mxcadManager/index.ts:94-108`\n- **严重程度**：🟡 中\n- **问题描述**：\n  mxcadManager 使用模块级 `let` 变量管理全局状态：\n  ```typescript\n  let currentFileInfo = null;\n  let cachedPersonalSpaceId = null;\n  let documentModified = false;\n  let pendingImages: PendingImage[] = [];\n  let currentMxwebUrl: string | null = null;\n  let navigateFunction: ((path: string) => void) | null = null;\n  ```\n  React 组件通过 `setCurrentFileInfo()` 等 setter 函数修改这些变量。这不是 Zustand store，也不是 React Context，而是一个独立的全局可变状态。问题：\n  1. 状态修改不会触发 React 重渲染，依赖 CustomEvent 手动同步\n  2. 多组件实例场景下状态共享不可预测\n  3. React Strict Mode 下模块级变量不会在 unmount 时重置\n- **修复建议**：\n  1. 将 mxcadManager 的状态迁移到 Zustand store（如 `useCadStore`）\n  2. 或在 mxcadManager 内部使用发布-订阅模式触发状态更新\n- **是否需要用户确认**：否\n\n### 🟡 问题 19：mxcadManager/index.ts 巨型文件（2367 行）\n\n- **文件**：`src/services/mxcadManager/index.ts`\n- **行数**：2367 行\n- **严重程度**：🟡 中\n- **问题描述**：\n  虽然有子模块拆分（mxcadSave、mxcadThumbnail 等），但 index.ts 仍然有 2367 行，包含了：\n  - 导航逻辑（NAVIGATION_PATHS + calculateReturnPath 等）\n  - 文件上传逻辑\n  - 命令处理（返回命令、保存命令、打开文件命令等）\n  - DOM 对话框（showUnsavedChangesDialog 用 innerHTML 创建）\n  - 主题同步逻辑\n  - 浏览器关闭保护\n- **修复建议**：\n  1. 提取 `mxcadNavigation.ts` — 导航路径和返回计算\n  2. 提取 `mxcadCommands.ts` — 命令注册和处理\n  3. 提取 `mxcadDialogs.ts` — DOM 对话框\n  4. 提取 `mxcadFileUpload.ts` — 文件上传逻辑\n  5. index.ts 只保留核心的 MxCADManager 类和重新导出\n- **是否需要用户确认**：否\n\n### 🟡 问题 20：showUnsavedChangesDialog 使用 innerHTML 构造 DOM\n\n- **文件**：`src/services/mxcadManager/index.ts:166-332`\n- **行号**：第 166-332 行\n- **严重程度**：🟡 中\n- **问题描述**：\n  未保存更改确认对话框通过纯 DOM 操作（innerHTML + addEventListener）创建，不经过 React。虽然功能可用，但：\n  1. 样式通过内联 `style` 字符串维护，无法享受 Tailwind CSS 和设计系统\n  2. 不受 React 生命周期管理，可能导致内存泄漏（事件监听器清理依赖 setTimeout）\n  3. 不符合项目中其他 Modal 使用 Radix UI 的统一模式\n- **修复建议**：使用 React Portal 渲染对话框组件，保持与其他 Modal 一致的模式\n- **是否需要用户确认**：否\n\n---\n\n## 总结\n\n| # | 类别 | 问题 | 文件 | 行号 | 严重程度 | 需确认 |\n|---|------|------|------|------|----------|--------|\n| 1 | 组件拆分 | CADEditorDirect 巨型组件 (1331行) | CADEditorDirect.tsx | 1-1331 | 🔴 严重 | 否 |\n| 2 | 组件拆分 | Profile 巨型组件 (1132行) | Profile.tsx | 1-1132 | 🟠 高 | 否 |\n| 3 | 组件拆分 | 超 300 行组件清单 (8 个超 500 行) | 多个文件 | — | 🟡 中 | 部分 |\n| 4 | 组件拆分 | FileSystemContent.tsx 重复 | 两个文件 | — | 🟡 中 | 是 |\n| 5 | 状态管理 | personalSpaceId 不应在全局 store | fileSystemStore.ts | 17-27 | 🟡 中 | 否 |\n| 6 | 状态管理 | notificationStore 与 uiStore.toasts 重叠 | 两个 store | — | 🟢 低 | 是 |\n| 7 | 状态管理 | 全局 loading 状态未使用 | uiStore.ts | 33-38 | 🟢 低 | 否 |\n| 8 | 路由设计 | 路由守卫内联定义 | App.tsx | 77-124 | 🟡 中 | 否 |\n| 9 | 路由设计 | 重定向路由嵌套重复 | App.tsx | 多处 | 🟢 低 | 否 |\n| 10 | 关注点分离 | SidebarNodeList 直接调用 API | SidebarNodeList.tsx | 9-10 | 🟡 中 | 否 |\n| 11 | 关注点分离 | CADEditorDirect 中 10+ 处直接 API 调用 | CADEditorDirect.tsx | 多处 | 🔴 严重 | 否 |\n| 12 | 关注点分离 | FileSystemManager 残留直接 API 引用 | FileSystemManager/index.tsx | 26-29 | 🟡 中 | 否 |\n| 13 | 关注点分离 | 缺少 Service 封装层 | services/ 目录 | — | 🟢 低 | 否 |\n| 14 | API 类型 | 内联 `as unknown as` 绕过生成类型 | CADEditorDirect.tsx | 512-1147 | 🟡 中 | 否 |\n| 15 | API 类型 | Dashboard 中使用 `as FileSystemNode` 断言 | Dashboard.tsx | 506-518 | 🟡 中 | 否 |\n| 16 | API 类型 | mxcadManager 大量 any 类型 | mxcadManager/index.ts | — | 🟢 低 | 否 |\n| 17 | CAD 集成 | 模块级副作用注册 beforeunload | mxcadManager/index.ts | 157 | 🟠 高 | 否 |\n| 18 | CAD 集成 | 模块级 let 变量管理全局状态 | mxcadManager/index.ts | 94-108 | 🟡 中 | 否 |\n| 19 | CAD 集成 | mxcadManager/index.ts 巨型文件 (2367行) | mxcadManager/index.ts | 1-2367 | 🟡 中 | 否 |\n| 20 | CAD 集成 | innerHTML 构造对话框 | mxcadManager/index.ts | 166-332 | 🟡 中 | 否 |\n\n**统计**：🔴 2 严重 · 🟠 2 高 · 🟡 12 中 · 🟢 4 低 | 需用户确认 3 项\n\n### 优先修复路线\n\n| 优先级 | 问题编号 | 原因 |\n|--------|----------|------|\n| P0 | #1, #11 | CADEditorDirect 巨型组件 + 直接 API 调用，影响可维护性和测试性 |\n| P1 | #17 | 模块级副作用可能引入隐蔽的 bug |\n| P2 | #5, #18 | 全局状态管理混乱 |\n| P3 | #2, #19 | 巨型组件和文件的拆分（长期改进） |\n| P4 | #3, #4, #6, #14, #15, #20 | 代码质量和一致性改进 |\n"]