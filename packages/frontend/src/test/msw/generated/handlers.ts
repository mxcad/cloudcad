/**
 * This file is AUTO GENERATED by [msw-auto-mock](https://github.com/zoubingwu/msw-auto-mock)
 * Feel free to commit/edit it as you need.
 */
/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { HttpResponse, http } from 'msw';
import { faker } from '@faker-js/faker';

faker.seed(1);

const baseURL = '';
const MAX_ARRAY_LENGTH = 20;
const MAX_STRING_LENGTH = 42;

// Map to store counters for each API endpoint
const apiCounters = new Map<string, number>();

const next = (apiKey: string) => {
  let currentCount = apiCounters.get(apiKey) ?? 0;
  if (currentCount === Number.MAX_SAFE_INTEGER - 1) {
    currentCount = 0;
  }
  apiCounters.set(apiKey, currentCount + 1);
  return currentCount;
};

export const handlers = [
  http.get(
    `${baseURL}/api/v1/admin/storage/cleanup/stats`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/admin/storage/cleanup/stats`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/admin/storage/deleted-files/stats`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/admin/storage/deleted-files/stats`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/admin/storage/orphans/stats`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/admin/storage/orphans/stats`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/admin/auth/mfa/bind`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/admin/auth/mfa/bind`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/auth/mfa/setup`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAdminMfaControllerSetup200Response(), { status: 200 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/admin/auth/mfa/setup`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/admin/permissions/cache/cleanup`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getAdminControllerCleanupCache200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/admin/permissions/cache/cleanup`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/admin/storage/orphans/cleanup`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/admin/storage/orphans/cleanup`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/block/nodes/batch-copy`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerBatchCopyBlockNodes201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/block/nodes/batch-copy`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/block/nodes/batch-delete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerBatchDeleteBlockNodes200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/block/nodes/batch-delete`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/block/nodes/batch-move`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerBatchMoveBlockNodes200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/block/nodes/batch-move`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/drawing/nodes/batch-copy`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerBatchCopyDrawingNodes201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/nodes/batch-copy`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/drawing/nodes/batch-delete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerBatchDeleteDrawingNodes200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/nodes/batch-delete`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/drawing/nodes/batch-move`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerBatchMoveDrawingNodes200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/nodes/batch-move`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/file-system/trash/items`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getTrashControllerPermanentlyDeleteTrashItems200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/file-system/trash/items`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/admin/billing/orders`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/admin/billing/orders`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/admin/billing/refund-applications`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/admin/billing/refund-applications`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/admin/permissions/cache`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAdminControllerGetCacheStats200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/admin/permissions/cache`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/stats/overview`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAdminControllerGetOverview200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/admin/stats/overview`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/stats/purchases`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAdminControllerGetPurchaseStats200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/admin/stats/purchases`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/admin/stats/registrations`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAdminControllerGetRegistrationStats200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/admin/stats/registrations`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/admin/tasks/runs`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getTaskRunControllerListRuns200Response(), { status: 200 }],
      [undefined, { status: 403 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/tasks/runs`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/tasks/tasks`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getTaskRunControllerListTasks200Response(), { status: 200 }],
      [undefined, { status: 403 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/tasks/tasks`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/vip/config-keys`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/admin/vip/config-keys`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/vip/durations`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/vip/durations`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/vip/tiers`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/vip/tiers`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/auth/wechat/login`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerGetWechatAuthUrl200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/auth/wechat/login`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/auth/wechat/poll-transaction`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerPollWechatTransaction200Response(),
          { status: 200 },
        ],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/auth/wechat/poll-transaction`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/batch-download/tasks`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getBatchDownloadControllerGetUserTasksdefaultResponse(),
          { status: 500 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/batch-download/tasks`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/projects/trash`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectControllerGetDeletedProjects200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/projects/trash`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/categories`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/categories`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/resolve-path`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerResolveBlockPath200Response(),
          { status: 200 },
        ],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/resolve-path`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/categories`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/categories`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/resolve-path`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerResolveDrawingPath200Response(),
          { status: 200 },
        ],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/resolve-path`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/mxcad/conversion/history`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getConversionTaskControllerListHistorydefaultResponse(),
          { status: 500 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/mxcad/conversion/history`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/mxcad/conversion/quota`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getConversionTaskControllerGetQuotadefaultResponse(),
        { status: 500 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/mxcad/conversion/quota`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/mxcad/conversion/tasks`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getConversionTaskControllerListTasksdefaultResponse(),
        { status: 500 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/mxcad/conversion/tasks`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/public-file/ext-reference/check`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPublicFileControllerCheckExtReference200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/public-file/ext-reference/check`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/roles/project-roles/system`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getRolesControllerGetSystemProjectRoles200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/roles/project-roles/system`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/users/profile/me`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerGetProfile200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/users/profile/me`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/users/search/by-email`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerSearchByEmail200Response(), { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/users/search/by-email`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/users/stats/me`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerGetDashboardStats200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/users/stats/me`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/vip/tiers/registry`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/vip/tiers/registry`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(`${baseURL}/api/v1/users/profile/me`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerUpdateProfile200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`patch /api/v1/users/profile/me`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/auth/login`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAdminAuthControllerLogin200Response(), { status: 200 }],
      [undefined, { status: 401 }],
      [undefined, { status: 403 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/admin/auth/login`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/admin/billing/manual-complete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getBillingAdminControllerManualComplete201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/admin/billing/manual-complete`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/admin/billing/refund`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getBillingAdminControllerRefund201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/admin/billing/refund`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/admin/security-attempts/blacklist`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 400 }],
        [undefined, { status: 409 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/admin/security-attempts/blacklist`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/admin/security-attempts/whitelist`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 400 }],
        [undefined, { status: 409 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/admin/security-attempts/whitelist`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/admin/storage/cleanup`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/admin/storage/cleanup`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/tasks/run`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getTaskRunControllerRunTask200Response(), { status: 200 }],
      [undefined, { status: 403 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/admin/tasks/run`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/vip/config-keys`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 201 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/admin/vip/config-keys`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/vip/durations`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 201 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/admin/vip/durations`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/vip/tiers`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 201 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`post /api/v1/admin/vip/tiers`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/auth/session-transfer/consume`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getSessionTransferControllerConsume200Response(),
          { status: 200 },
        ],
        [undefined, { status: 401 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/session-transfer/consume`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/auth/wechat/bind`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerBindWechat200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/wechat/bind`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/wechat/unbind`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerUnbindWechat200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/wechat/unbind`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/billing/orders/auto`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getBillingControllerAutoCreateOrder201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/billing/orders/auto`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/device/oauth/token`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getDeviceAuthControllerPollForToken200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/device/oauth/token`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/file-system/batch-download/merge-zip`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/batch-download/merge-zip`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/batch-download/single-file`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/batch-download/single-file`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/download/batch-zip`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/download/batch-zip`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/batch-copy`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerBatchCopyNodes201Response(), { status: 201 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/batch-copy`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/batch-delete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerBatchDeleteNodes200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/batch-delete`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/batch-move`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerBatchMoveNodes200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/batch-move`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/create-drawing`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerCreateDrawing201Response(), { status: 201 }],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/create-drawing`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/lookup`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerLookupNodes200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/lookup`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/trash/restore`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getTrashControllerRestoreTrashItems200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/trash/restore`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/library/block/folders`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getLibraryControllerCreateBlockFolder201Response(),
        { status: 201 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/library/block/folders`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/library/block/save-as`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/library/block/save-as`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/library/drawing/folders`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerCreateDrawingFolder201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/folders`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/drawing/save-as`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/save-as`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/mxcad/conversion/tasks`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getConversionTaskControllerSubmitTaskdefaultResponse(),
        { status: 500 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/mxcad/conversion/tasks`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/mxcad/files/chunkisExist`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMxcadUploadControllerCheckChunkExist200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/files/chunkisExist`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/files/fileisExist`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMxcadUploadControllerCheckFileExist200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/files/fileisExist`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/files/uploadFiles`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMxcadUploadControllerUploadFile200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/files/uploadFiles`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/notices/stream/ticket`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getNoticeCenterControllerIssueTicket200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/notices/stream/ticket`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/public-file/ext-reference/upload`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPublicFileControllerUploadExtReference200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/public-file/ext-reference/upload`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/users/me/restore`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerRestoreAccount200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/users/me/restore`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/users/profile/avatar`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerUploadAvatar200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/users/profile/avatar`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(
    `${baseURL}/api/v1/cache-monitor/pattern`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/cache-monitor/pattern`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(`${baseURL}/api/v1/cache-monitor/value`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`delete /api/v1/cache-monitor/value`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(`${baseURL}/api/v1/cache-monitor/values`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`delete /api/v1/cache-monitor/values`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(`${baseURL}/api/v1/file-system/trash`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getTrashControllerClearTrash200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`delete /api/v1/file-system/trash`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/auth/wechat/callback`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/auth/wechat/callback`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/backups`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getBackupControllerListBackups200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/backups`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/ip-blacklist`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getIpBlacklistControllerList200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/ip-blacklist`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/ip-whitelist`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getIpWhitelistControllerList200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/ip-whitelist`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/security-attempts`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getSecurityAccessAttemptControllerList200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/admin/security-attempts`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/audit/logs`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/audit/logs`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/audit/statistics`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/audit/statistics`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/auth/profile`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerGetProfile200Response(), { status: 200 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/auth/profile`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/billing/membership`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/billing/membership`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/billing/orders`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/billing/orders`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/cache-monitor/health`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/cache-monitor/health`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/cache-monitor/performance`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/cache-monitor/performance`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/cache-monitor/performance-trend`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getCacheMonitorControllerGetPerformanceTrend200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/cache-monitor/performance-trend`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/cache-monitor/size-trend`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getCacheMonitorControllerGetSizeTrend200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/cache-monitor/size-trend`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/cache-monitor/stats`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/cache-monitor/stats`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/cache-monitor/summary`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getCacheMonitorControllerGetSummary200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/cache-monitor/summary`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/cache-monitor/value`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/cache-monitor/value`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/cache-monitor/warnings`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getCacheMonitorControllerGetWarnings200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/cache-monitor/warnings`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/conversion-monitor/stats`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getConversionMonitorControllerGetStats200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/conversion-monitor/stats`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/conversion-monitor/tasks`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getConversionMonitorControllerListTasks200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/conversion-monitor/tasks`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/personal-space`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectControllerGetPersonalSpace200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/personal-space`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/file-system/projects`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getProjectControllerGetProjects200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/file-system/projects`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/file-system/quota`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getProjectControllerGetStorageQuota200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/file-system/quota`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/file-system/resolve-path`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerResolvePath200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/resolve-path`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/file-system/search`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNodeControllerSearch200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/file-system/search`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/file-system/trash`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getTrashControllerGetTrash200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/file-system/trash`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/library/block`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getLibraryControllerGetBlockLibrary200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/library/block`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/library/drawing`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getLibraryControllerGetDrawingLibrary200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/library/drawing`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/notices/current`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNoticeCenterControllerGetCurrent200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/notices/current`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/queue/stats`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getQueueControllerGetQueueStats200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/queue/stats`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/runtime-config/definitions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getRuntimeConfigControllerGetDefinitions200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/runtime-config/definitions`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/runtime-config/public`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getRuntimeConfigControllerGetPublicConfigs200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/runtime-config/public`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/user-cleanup/stats`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUserCleanupControllerGetStats200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/user-cleanup/stats`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/users/search`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerSearchUsers200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/users/search`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/version-control/history`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getVersionControlControllerGetFileHistory200Response(),
        { status: 200 },
      ],
      [undefined, { status: 400 }],
      [undefined, { status: 401 }],
      [undefined, { status: 403 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/version-control/history`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/vip/durations`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/vip/durations`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/vip/tiers`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/vip/tiers`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/backup`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getBackupControllerTriggerBackup200Response(), { status: 200 }],
      [undefined, { status: 500 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/admin/backup`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/ip-blacklist`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getIpBlacklistControllerCreate201Response(), { status: 201 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/admin/ip-blacklist`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/admin/ip-whitelist`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getIpWhitelistControllerCreate201Response(), { status: 201 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/admin/ip-whitelist`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/audit/cleanup`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/audit/cleanup`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/audit/export`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`post /api/v1/audit/export`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/bind-email`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerSendBindEmailCode200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/bind-email`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/auth/bind-email-and-login`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerBindEmailAndLogin200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/bind-email-and-login`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/auth/bind-phone`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerBindPhone200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/bind-phone`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/auth/bind-phone-and-login`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerBindPhoneAndLogin200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/bind-phone-and-login`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/auth/check-field`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getAuthControllerCheckFieldUniqueness200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/check-field`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/forgot-password`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerForgotPassword200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/auth/forgot-password`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/login`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerLogin200Response(), { status: 200 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/login`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/login-phone`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerLoginByPhone200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 412 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/login-phone`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/logout`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/logout`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/rebind-email`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerRebindEmail200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/rebind-email`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/rebind-phone`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerRebindPhone200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/rebind-phone`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/refresh`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerRefreshToken200Response(), { status: 200 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/refresh`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/register`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerRegister201Response(), { status: 201 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/register`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/register-phone`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerRegisterByPhone201Response(), { status: 201 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/auth/register-phone`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/auth/resend-verification`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerResendVerification200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/resend-verification`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/auth/reset-password`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerResetPassword200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/auth/reset-password`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/send-sms-code`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerSendSmsCode200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/send-sms-code`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/auth/send-unbind-email-code`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerSendUnbindEmailCode200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/send-unbind-email-code`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/auth/send-unbind-phone-code`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerSendUnbindPhoneCode200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/send-unbind-phone-code`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/auth/send-verification`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerSendVerification200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/auth/send-verification`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/session-transfer`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getSessionTransferControllerCreate200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/auth/session-transfer`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/unbind-email`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerUnbindEmail200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/unbind-email`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/unbind-phone`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerUnbindPhone200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/unbind-phone`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/verify-bind-email`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerVerifyBindEmail200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 401 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/auth/verify-bind-email`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/verify-email`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerVerifyEmail200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/verify-email`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/auth/verify-email-and-register-phone`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerVerifyEmailAndRegisterPhone201Response(),
          { status: 201 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 409 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/verify-email-and-register-phone`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/auth/verify-phone`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerVerifyPhone200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/auth/verify-phone`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/auth/verify-sms-code`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAuthControllerVerifySmsCode200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/auth/verify-sms-code`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/auth/verify-unbind-email-code`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerVerifyUnbindEmailCode200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/verify-unbind-email-code`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/auth/verify-unbind-phone-code`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getAuthControllerVerifyUnbindPhoneCode200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/auth/verify-unbind-phone-code`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/billing/orders`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getBillingControllerCreateOrder201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/billing/orders`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/cache-monitor/cleanup`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/cache-monitor/cleanup`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/cache-monitor/refresh`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/cache-monitor/refresh`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/cache-monitor/value`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/cache-monitor/value`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/device/authorize`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getDeviceAuthControllerAuthorizeDevice200Response(),
        { status: 200 },
      ],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/device/authorize`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/device/code`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getDeviceAuthControllerRequestDeviceCode200Response(),
        { status: 200 },
      ],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/device/code`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/file-system/batch-download`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/batch-download`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/file-system/nodes`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNodeControllerCreateNode201Response(), { status: 201 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/file-system/nodes`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/file-system/projects`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getProjectControllerCreateProject201Response(), { status: 201 }],
      [undefined, { status: 400 }],
      [undefined, { status: 403 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/file-system/projects`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/files/upload-token`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getUploadTokenControllerCreateUploadToken200Response(),
        { status: 200 },
      ],
      [undefined, { status: 400 }],
      [undefined, { status: 401 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/files/upload-token`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/font-management/batch-delete`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getFontsControllerBatchDeleteFonts200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/font-management/batch-delete`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/font-management/upload`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 201 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/font-management/upload`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/mxcad/save-as`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getSaveControllerSaveMxwebAs200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/mxcad/save-as`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/public-file/convert`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/public-file/convert`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/roles/project-roles`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getRolesControllerCreateProjectRole201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/roles/project-roles`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/user-cleanup/trigger`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getUserCleanupControllerTriggerCleanup200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/user-cleanup/trigger`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/users/change-password`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerChangePassword200Response(), { status: 200 }],
      [undefined, { status: 400 }],
      [undefined, { status: 401 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/users/change-password`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/users/deactivate-account`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getUsersControllerDeactivateAccount200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/users/deactivate-account`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/health/db`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 503 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/health/db`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/health/full`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getHealthControllerCheck200Response(), { status: 200 }],
      [await getHealthControllerCheck503Response(), { status: 503 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/health/full`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/health/live`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/health/live`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/health/public`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 503 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/health/public`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/health/storage`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 503 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/health/storage`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/alert`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAlertControllerList200Response(), { status: 200 }],
      [undefined, { status: 403 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/alert`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/font-management`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/font-management`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/notices`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNoticeCenterControllerListAll200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/notices`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/roles`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getRolesControllerFindAll200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/roles`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/runtime-config`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getRuntimeConfigControllerGetAllConfigs200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/runtime-config`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/shares`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getShareControllerListShares200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/shares`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/users`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerFindAll200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/users`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/notices`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNoticeCenterControllerCreate201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/notices`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/roles`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getRolesControllerCreate201Response(), { status: 201 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/roles`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/shares`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getShareControllerCreateShare201Response(), { status: 201 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/shares`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/users`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerCreate201Response(), { status: 201 }],
      [undefined, { status: 400 }],
      [undefined, { status: 409 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/users`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/health`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getHealthControllerCheckFull200Response(), { status: 200 }],
      [await getHealthControllerCheckFull503Response(), { status: 503 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/health`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] = resultArray[next(`get /api/v1`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(
    `${baseURL}/api/v1/admin/permissions/cache/user/:userId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getAdminControllerClearUserCache200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/admin/permissions/cache/user/:userId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/admin/vip/tiers/:id/permanent`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/admin/vip/tiers/:id/permanent`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/batch-download/folder/:nodeId/files`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/batch-download/folder/:nodeId/files`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/nodes/:nodeId/download`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerDownloadBlockNode200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/nodes/:nodeId/download`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/nodes/:nodeId/thumbnail`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 204 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/nodes/:nodeId/thumbnail`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/nodes/:nodeId/download`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerDownloadDrawingNode200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/nodes/:nodeId/download`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/nodes/:nodeId/thumbnail`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 204 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/nodes/:nodeId/thumbnail`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/mxcad/conversion/nodes/:nodeId/status`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getConversionStatusControllerGetConversionStatusdefaultResponse(),
          { status: 500 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/mxcad/conversion/nodes/:nodeId/status`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/mxcad/nodes/:nodeId/thumbnail/check`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getThumbnailControllerCheckThumbnail200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/mxcad/nodes/:nodeId/thumbnail/check`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(
    `${baseURL}/api/v1/file-system/projects/:projectId/members/batch`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`patch /api/v1/file-system/projects/:projectId/members/batch`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/admin/billing/refund-applications/:id/approve`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getBillingAdminControllerApproveRefundApplication201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/admin/billing/refund-applications/:id/approve`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/admin/billing/refund-applications/:id/reject`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getBillingAdminControllerRejectRefundApplication201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/admin/billing/refund-applications/:id/reject`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/projects/:projectId/members/batch`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 201 }],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/projects/:projectId/members/batch`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/block/nodes/:nodeId/copy`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getLibraryControllerCopyBlockNode201Response(), { status: 201 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/block/nodes/:nodeId/copy`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/block/nodes/:nodeId/move`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getLibraryControllerMoveBlockNode200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/block/nodes/:nodeId/move`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/drawing/nodes/:nodeId/copy`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerCopyDrawingNode201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/nodes/:nodeId/copy`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/drawing/nodes/:nodeId/move`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerMoveDrawingNode200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/nodes/:nodeId/move`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/conversion/nodes/:nodeId/convert`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/conversion/nodes/:nodeId/convert`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/conversion/tasks/:taskId/cancel`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/conversion/tasks/:taskId/cancel`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/conversion/tasks/:taskId/retry`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/conversion/tasks/:taskId/retry`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.put(
    `${baseURL}/api/v1/admin/vip/tiers/:id/configs`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('put')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`put /api/v1/admin/vip/tiers/:id/configs`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/admin/vip/config-keys/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/admin/vip/config-keys/:id`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/admin/vip/durations/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/admin/vip/durations/:id`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(`${baseURL}/api/v1/admin/vip/tiers/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`delete /api/v1/admin/vip/tiers/:id`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(
    `${baseURL}/api/v1/file-system/projects/:projectId/trash`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getTrashControllerClearProjectTrash200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/file-system/projects/:projectId/trash`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/library/block/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/library/block/nodes/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/library/drawing/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/library/drawing/nodes/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/roles/project-roles/:id/permissions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/roles/project-roles/:id/permissions`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/admin/vip/durations/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/admin/vip/durations/:id`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/admin/vip/tiers/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/admin/vip/tiers/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/file-system/batch-download/:taskId/download`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/batch-download/:taskId/download`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/batch-download/:taskId/progress`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getBatchDownloadControllerGetProgressdefaultResponse(),
          { status: 500 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/batch-download/:taskId/progress`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/children`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerGetChildren200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId/children`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/download`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId/download`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/download-with-format`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId/download-with-format`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/history`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId/history`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/parent-context`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerGetParentContext200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId/parent-context`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/root`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerGetRootNode200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId/root`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/thumbnail`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 204 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId/thumbnail`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/projects/:projectId/members`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMemberControllerGetProjectMembers200Response(),
          { status: 200 },
        ],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/projects/:projectId/members`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/projects/:projectId/permissions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMemberControllerGetUserProjectPermissions200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/projects/:projectId/permissions`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/projects/:projectId/trash`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getTrashControllerGetProjectTrash200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/projects/:projectId/trash`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/quota/project/:projectId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectControllerGetProjectQuota200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/quota/project/:projectId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/all-files/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerGetBlockAllFiles200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/all-files/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/children/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerGetBlockChildren200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/children/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/filesData/:path`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getLibraryControllerGetBlockFile200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/filesData/:path`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/block/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getLibraryControllerGetBlockNode200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/block/nodes/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/all-files/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerGetDrawingAllFiles200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/all-files/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/children/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerGetDrawingChildren200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/children/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/filesData/:path`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerGetDrawingFile200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/filesData/:path`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/library/drawing/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerGetDrawingNode200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/library/drawing/nodes/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/roles/project-roles/project/:projectId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getRolesControllerGetProjectRolesByProject200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/roles/project-roles/project/:projectId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.options(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/download`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('options')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`options /api/v1/file-system/nodes/:nodeId/download`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(
    `${baseURL}/api/v1/library/block/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerRenameBlockNode200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`patch /api/v1/library/block/nodes/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(
    `${baseURL}/api/v1/library/drawing/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getLibraryControllerRenameDrawingNode200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`patch /api/v1/library/drawing/nodes/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/billing/orders/:orderNo/mock-scan`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/billing/orders/:orderNo/mock-scan`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/billing/orders/:orderNo/query`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/billing/orders/:orderNo/query`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/billing/orders/:orderNo/refund-apply`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/billing/orders/:orderNo/refund-apply`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/billing/orders/:orderNo/repay`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getBillingControllerRepayOrder201Response(), { status: 201 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/billing/orders/:orderNo/repay`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/batch-download/:taskId/cancel`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/batch-download/:taskId/cancel`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/batch-download/:taskId/retry`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/batch-download/:taskId/retry`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/batch-download/:taskId/retry-failed`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/batch-download/:taskId/retry-failed`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/copy`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerCopyNode201Response(), { status: 201 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/:nodeId/copy`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/move`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerMoveNode200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/:nodeId/move`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/:nodeId/restore`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerRestoreNode200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/:nodeId/restore`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/nodes/:parentId/folders`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerCreateFolder201Response(), { status: 201 }],
        [undefined, { status: 400 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/nodes/:parentId/folders`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/projects/:projectId/members`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMemberControllerAddProjectMember201Response(),
          { status: 201 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/projects/:projectId/members`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/file-system/projects/:projectId/transfer`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/file-system/projects/:projectId/transfer`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/block/save/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/block/save/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/library/drawing/save/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/library/drawing/save/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/file/:nodeId/check-reference`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMxcadExternalRefControllerCheckExternalReference200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/file/:nodeId/check-reference`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/file/:nodeId/refresh-external-references`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMxcadExternalRefControllerRefreshExternalReferences200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/file/:nodeId/refresh-external-references`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/nodes/:nodeId/thumbnail`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getThumbnailControllerUploadThumbnail200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 500 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/nodes/:nodeId/thumbnail`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/roles/project-roles/:id/permissions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/roles/project-roles/:id/permissions`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.put(
    `${baseURL}/api/v1/admin/vip/config-keys/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('put')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`put /api/v1/admin/vip/config-keys/:id`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.put(`${baseURL}/api/v1/admin/vip/durations/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('put')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`put /api/v1/admin/vip/durations/:id`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.put(`${baseURL}/api/v1/admin/vip/tiers/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('put')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`put /api/v1/admin/vip/tiers/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.put(
    `${baseURL}/api/v1/file-system/projects/:projectId/transfer-settings`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('put')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectControllerUpdateTransferSettings200Response(),
          { status: 200 },
        ],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(
            `put /api/v1/file-system/projects/:projectId/transfer-settings`
          ) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(`${baseURL}/api/v1/admin/backups/:name`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getBackupControllerDeleteBackup200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`delete /api/v1/admin/backups/:name`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(
    `${baseURL}/api/v1/admin/ip-blacklist/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/admin/ip-blacklist/:id`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/admin/ip-whitelist/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 400 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/admin/ip-whitelist/:id`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/file-system/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerDeleteNode200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/file-system/nodes/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/file-system/projects/:projectId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getProjectControllerDeleteProject200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/file-system/projects/:projectId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/roles/:id/permissions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getRolesControllerRemovePermissions200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/roles/:id/permissions`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/roles/project-roles/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/roles/project-roles/:id`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/audit/logs/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`get /api/v1/audit/logs/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/audit/project/:projectId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/audit/project/:projectId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/billing/orders/:orderNo`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/billing/orders/:orderNo`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/file-system/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerGetNode200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/nodes/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/file-system/projects/:projectId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getProjectControllerGetProject200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/file-system/projects/:projectId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/font-management/download/:fileName`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/font-management/download/:fileName`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/mxcad/file/:path`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/mxcad/file/:path`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/mxcad/files/:storageKey`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/mxcad/files/:storageKey`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/mxcad/filesData/:path`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 202 }],
      [undefined, { status: 204 }],
      [undefined, { status: 401 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/mxcad/filesData/:path`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/mxcad/preloading/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMxcadExternalRefControllerGetPreloadingData200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/mxcad/preloading/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/public-file/access/:filename`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPublicFileControllerAccessFileByHashPattern200Response(),
          { status: 200 },
        ],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/public-file/access/:filename`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/public-file/preloading/:hash`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getPublicFileControllerGetPreloadingData200Response(),
          { status: 200 },
        ],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/public-file/preloading/:hash`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(`${baseURL}/api/v1/roles/:id/permissions`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getRolesControllerGetRolePermissions200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`get /api/v1/roles/:id/permissions`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/shares/:token/node`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getShareControllerResolveShareNode200Response(), { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/shares/:token/node`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/shares/file/:fileId`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getShareControllerGetFileShares200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/shares/file/:fileId`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/users/avatar/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 304 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/users/avatar/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/version-control/file/:revision`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getVersionControlControllerGetFileContentAtRevision200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/version-control/file/:revision`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/version-control/list/:revision`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getVersionControlControllerListDirectoryAtRevision200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/version-control/list/:revision`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.head(`${baseURL}/api/v1/mxcad/file/:path`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('head')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`head /api/v1/mxcad/file/:path`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.head(`${baseURL}/api/v1/mxcad/filesData/:path`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('head')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`head /api/v1/mxcad/filesData/:path`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(`${baseURL}/api/v1/alert/:id/resolve`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getAlertControllerResolve200Response(), { status: 200 }],
      [undefined, { status: 403 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`patch /api/v1/alert/:id/resolve`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(
    `${baseURL}/api/v1/file-system/nodes/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getNodeControllerUpdateNode200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`patch /api/v1/file-system/nodes/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(
    `${baseURL}/api/v1/file-system/projects/:projectId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getProjectControllerUpdateProject200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`patch /api/v1/file-system/projects/:projectId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(
    `${baseURL}/api/v1/roles/project-roles/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getRolesControllerUpdateProjectRole200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`patch /api/v1/roles/project-roles/:id`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(`${baseURL}/api/v1/users/:id/membership`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerUpdateMembership200Response(), { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`patch /api/v1/users/:id/membership`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(`${baseURL}/api/v1/users/:id/status`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerUpdateStatus200Response(), { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`patch /api/v1/users/:id/status`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/mxcad/savemxweb/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getSaveControllerSaveMxwebToNode200Response(), { status: 200 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/savemxweb/:nodeId`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/up_ext_reference_dwg/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/up_ext_reference_dwg/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/mxcad/up_ext_reference_image/:nodeId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 201 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/mxcad/up_ext_reference_image/:nodeId`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/notices/:id/publish`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNoticeCenterControllerPublish200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/notices/:id/publish`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(`${baseURL}/api/v1/notices/:id/retract`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNoticeCenterControllerRetract200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/notices/:id/retract`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/projects/:projectId/project-roles`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectRolesControllerCreateProjectRole201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/projects/:projectId/project-roles`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/roles/:id/permissions`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getRolesControllerAddPermissions200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[
        next(`post /api/v1/roles/:id/permissions`) % resultArray.length
      ];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/runtime-config/:key/reset`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getRuntimeConfigControllerResetConfig201Response(),
          { status: 201 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/runtime-config/:key/reset`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/users/:id/avatar`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerUploadUserAvatar200Response(), { status: 200 }],
      [undefined, { status: 400 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/users/:id/avatar`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.post(
    `${baseURL}/api/v1/users/:id/delete-immediately`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`post /api/v1/users/:id/delete-immediately`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(`${baseURL}/api/v1/users/:id/restore`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`post /api/v1/users/:id/restore`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(
    `${baseURL}/api/v1/font-management/:fileName`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/font-management/:fileName`) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(`${baseURL}/api/v1/roles/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [[undefined, { status: 200 }]] as [
      any,
      { status: number },
    ][];

    const [body, init] =
      resultArray[next(`delete /api/v1/roles/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(`${baseURL}/api/v1/shares/:token`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 403 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`delete /api/v1/shares/:token`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.delete(`${baseURL}/api/v1/users/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('delete')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`delete /api/v1/users/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/roles/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getRolesControllerFindOne200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/roles/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/runtime-config/:key`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getRuntimeConfigControllerGetConfig200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/runtime-config/:key`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/shares/:token`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getShareControllerResolveShare200Response(), { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/shares/:token`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(`${baseURL}/api/v1/users/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerFindOne200Response(), { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`get /api/v1/users/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(`${baseURL}/api/v1/notices/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getNoticeCenterControllerUpdate200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`patch /api/v1/notices/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(`${baseURL}/api/v1/roles/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getRolesControllerUpdate200Response(), { status: 200 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`patch /api/v1/roles/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(`${baseURL}/api/v1/shares/:token`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [undefined, { status: 200 }],
      [undefined, { status: 403 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`patch /api/v1/shares/:token`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.patch(`${baseURL}/api/v1/users/:id`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [await getUsersControllerUpdate200Response(), { status: 200 }],
      [undefined, { status: 404 }],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`patch /api/v1/users/:id`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.put(`${baseURL}/api/v1/runtime-config/:key`, async ({ request }) => {
    const shouldEchoRequestBody = false;
    let requestJson = null;
    if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('put')) {
      try {
        requestJson = await request.clone().json();
      } catch (e) {
        requestJson = null;
      }
    }

    const resultArray = [
      [
        await getRuntimeConfigControllerUpdateConfig200Response(),
        { status: 200 },
      ],
    ] as [any, { status: number }][];

    const [body, init] =
      resultArray[next(`put /api/v1/runtime-config/:key`) % resultArray.length];
    const responseJson =
      requestJson && body && typeof body === 'object' && !Array.isArray(body)
        ? { ...body, ...requestJson }
        : body;
    return HttpResponse.json(responseJson, init);
  }),
  http.get(
    `${baseURL}/api/v1/file-system/batch-download/:taskId/items/:itemIndex/download`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(
            `get /api/v1/file-system/batch-download/:taskId/items/:itemIndex/download`
          ) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/file-system/projects/:projectId/members/:userId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMemberControllerRemoveProjectMember200Response(),
          { status: 200 },
        ],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(
            `delete /api/v1/file-system/projects/:projectId/members/:userId`
          ) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/projects/:projectId/project-roles/:id/permissions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectRolesControllerRemoveProjectRolePermissions200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(
            `delete /api/v1/projects/:projectId/project-roles/:id/permissions`
          ) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/mxcad/file/:nodeId/download-external-ref/:fileName`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(
            `get /api/v1/mxcad/file/:nodeId/download-external-ref/:fileName`
          ) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(
    `${baseURL}/api/v1/file-system/projects/:projectId/members/:userId`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getMemberControllerUpdateProjectMember200Response(),
          { status: 200 },
        ],
        [undefined, { status: 400 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(
            `patch /api/v1/file-system/projects/:projectId/members/:userId`
          ) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.post(
    `${baseURL}/api/v1/projects/:projectId/project-roles/:id/permissions`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('post')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectRolesControllerAddProjectRolePermissions200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(
            `post /api/v1/projects/:projectId/project-roles/:id/permissions`
          ) % resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.delete(
    `${baseURL}/api/v1/projects/:projectId/project-roles/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (
        shouldEchoRequestBody &&
        ['post', 'put', 'patch'].includes('delete')
      ) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [[undefined, { status: 200 }]] as [
        any,
        { status: number },
      ][];

      const [body, init] =
        resultArray[
          next(`delete /api/v1/projects/:projectId/project-roles/:id`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/mxcad/external-ref-view/:nodeId/:fileName`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [undefined, { status: 200 }],
        [undefined, { status: 401 }],
        [undefined, { status: 403 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/mxcad/external-ref-view/:nodeId/:fileName`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.get(
    `${baseURL}/api/v1/public-file/access/:hash/:filename`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('get')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [await getPublicFileControllerAccessFile200Response(), { status: 200 }],
        [undefined, { status: 404 }],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`get /api/v1/public-file/access/:hash/:filename`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
  http.patch(
    `${baseURL}/api/v1/projects/:projectId/project-roles/:id`,
    async ({ request }) => {
      const shouldEchoRequestBody = false;
      let requestJson = null;
      if (shouldEchoRequestBody && ['post', 'put', 'patch'].includes('patch')) {
        try {
          requestJson = await request.clone().json();
        } catch (e) {
          requestJson = null;
        }
      }

      const resultArray = [
        [
          await getProjectRolesControllerUpdateProjectRole200Response(),
          { status: 200 },
        ],
      ] as [any, { status: number }][];

      const [body, init] =
        resultArray[
          next(`patch /api/v1/projects/:projectId/project-roles/:id`) %
            resultArray.length
        ];
      const responseJson =
        requestJson && body && typeof body === 'object' && !Array.isArray(body)
          ? { ...body, ...requestJson }
          : body;
      return HttpResponse.json(responseJson, init);
    }
  ),
];

export function getCacheMonitorControllerGetSummary200Response() {
  return {
    stats: {},
    healthStatus: {},
    performanceMetrics: {},
    timestamp: faker.date.anytime().toISOString(),
  };
}

export function getCacheMonitorControllerGetPerformanceTrend200Response() {
  return {
    timestamps: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    avgResponseTimes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errorRates: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getCacheMonitorControllerGetSizeTrend200Response() {
  return {
    L1: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    L2: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getCacheMonitorControllerGetWarnings200Response() {
  return {
    warnings: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getRuntimeConfigControllerGetPublicConfigs200Response() {
  return {};
}

export function getRuntimeConfigControllerGetAllConfigs200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      key: 'mailEnabled',
      value: {},
      type: 'boolean',
      category: 'mail',
      description: '邮件服务开关',
      isPublic: true,
      updatedBy: faker.lorem.words(),
      updatedAt: '2024-01-01T00:00:00.000Z',
    }));
  })();
}

export function getRuntimeConfigControllerGetDefinitions200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      key: 'mailEnabled',
      type: 'boolean',
      category: 'mail',
      description: '邮件服务开关',
      defaultValue: {},
      isPublic: true,
    }));
  })();
}

export function getRuntimeConfigControllerGetConfig200Response() {
  return {
    key: 'mailEnabled',
    value: {},
    type: 'boolean',
    category: 'mail',
    description: '邮件服务开关',
    isPublic: true,
    updatedBy: faker.lorem.words(),
    updatedAt: '2024-01-01T00:00:00.000Z',
  };
}

export function getRuntimeConfigControllerUpdateConfig200Response() {
  return {
    success: true,
  };
}

export function getRuntimeConfigControllerResetConfig201Response() {
  return {
    success: true,
  };
}

export function getTaskRunControllerListRuns200Response() {
  return {
    data: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        taskName: faker.person.fullName(),
        status: faker.helpers.arrayElement(['SUCCESS', 'FAILED']),
        startedAt: faker.date.anytime().toISOString(),
        finishedAt: {},
        durationMs: {},
        errorSummary: {},
        trigger: faker.helpers.arrayElement(['SCHEDULED', 'MANUAL']),
        triggeredBy: {},
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    pagination: { page: 1, limit: 20, total: 0, totalPages: 0 },
  };
}

export function getTaskRunControllerListTasks200Response() {
  return {
    data: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        taskName: faker.person.fullName(),
        description: faker.lorem.words(),
        schedule: {},
        scheduleLabel: {},
      }));
    })(),
  };
}

export function getTaskRunControllerRunTask200Response() {
  return {
    success: faker.datatype.boolean(),
    taskName: faker.person.fullName(),
    triggeredAt: faker.date.anytime().toISOString(),
  };
}

export function getAlertControllerList200Response() {
  return {
    data: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        source: faker.lorem.words(),
        messageKey: faker.lorem.words(),
        level: faker.helpers.arrayElement(['P0', 'P1', 'P2']),
        message: faker.lorem.words(),
        detail: {},
        status: faker.helpers.arrayElement(['OPEN', 'RESOLVED']),
        resolvedAt: {},
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
      }));
    })(),
    pagination: { page: 1, limit: 20, total: 0, totalPages: 0 },
  };
}

export function getAlertControllerResolve200Response() {
  return {
    id: faker.string.uuid(),
    source: faker.lorem.words(),
    messageKey: faker.lorem.words(),
    level: faker.helpers.arrayElement(['P0', 'P1', 'P2']),
    message: faker.lorem.words(),
    detail: {},
    status: faker.helpers.arrayElement(['OPEN', 'RESOLVED']),
    resolvedAt: {},
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getAuthControllerRegister201Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerLogin200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerRefreshToken200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerGetProfile200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.lorem.words(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getAuthControllerSendVerification200Response() {
  return {
    code: {
      message: '验证邮件已发送',
    },
    message: faker.lorem.words(),
    data: {
      message: '验证邮件已发送',
    },
    timestamp: faker.lorem.words(),
  };
}

export function getAuthControllerVerifyEmail200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerVerifyEmailAndRegisterPhone201Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerResendVerification200Response() {
  return {
    code: {
      message: '验证邮件已发送',
    },
    message: faker.lorem.words(),
    data: {
      message: '验证邮件已发送',
    },
    timestamp: faker.lorem.words(),
  };
}

export function getAuthControllerBindEmailAndLogin200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerBindPhoneAndLogin200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerVerifyPhone200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerForgotPassword200Response() {
  return {
    message: faker.lorem.words(),
    mailEnabled: faker.datatype.boolean(),
    smsEnabled: faker.datatype.boolean(),
    supportEmail: faker.internet.email(),
    supportPhone: faker.lorem.words(),
  };
}

export function getAuthControllerResetPassword200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getAuthControllerSendBindEmailCode200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getAuthControllerVerifyBindEmail200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getAuthControllerSendUnbindEmailCode200Response() {
  return { success: true, message: '验证码已发送到原邮箱' };
}

export function getAuthControllerVerifyUnbindEmailCode200Response() {
  return { success: true, message: '验证通过', token: 'xxx' };
}

export function getAuthControllerRebindEmail200Response() {
  return { success: true, message: '邮箱换绑成功' };
}

export function getAuthControllerSendSmsCode200Response() {
  return { success: true, message: '验证码已发送' };
}

export function getAuthControllerVerifySmsCode200Response() {
  return { valid: true, message: '验证成功' };
}

export function getAuthControllerRegisterByPhone201Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerLoginByPhone200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAuthControllerBindPhone200Response() {
  return { success: true, message: '手机号绑定成功' };
}

export function getAuthControllerSendUnbindPhoneCode200Response() {
  return { success: true, message: '验证码已发送到原手机号' };
}

export function getAuthControllerVerifyUnbindPhoneCode200Response() {
  return { success: true, message: '验证通过', token: 'xxx' };
}

export function getAuthControllerRebindPhone200Response() {
  return { success: true, message: '手机号换绑成功' };
}

export function getAuthControllerCheckFieldUniqueness200Response() {
  return { usernameExists: false, emailExists: false, phoneExists: false };
}

export function getAuthControllerGetWechatAuthUrl200Response() {
  return {
    authUrl: faker.internet.url(),
    state: faker.location.state(),
    transactionId: faker.string.uuid(),
  };
}

export function getAuthControllerPollWechatTransaction200Response() {
  return {
    status: faker.lorem.words(),
    action: faker.lorem.words(),
    accessToken: faker.lorem.words(),
    refreshToken: faker.lorem.words(),
    user: {},
    tempToken: faker.lorem.words(),
    error: faker.lorem.words(),
    restored: faker.datatype.boolean(),
    errorCode: faker.lorem.words(),
    graceDays: faker.number.int(),
    cleanupDays: faker.number.int(),
  };
}

export function getAuthControllerBindWechat200Response() {
  return { success: true, message: '微信绑定成功' };
}

export function getAuthControllerUnbindWechat200Response() {
  return { success: true, message: '微信解绑成功' };
}

export function getAuthControllerUnbindEmail200Response() {
  return { success: true, message: '邮箱解绑成功' };
}

export function getAuthControllerUnbindPhone200Response() {
  return { success: true, message: '手机号解绑成功' };
}

export function getAdminAuthControllerLogin200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getAdminMfaControllerSetup200Response() {
  return {
    secret: 'JBSWY3DPEHPK3PXP',
    otpauthUrl: 'otpauth://totp/CloudCAD:admin?secret=...&issuer=CloudCAD',
  };
}

export function getSessionTransferControllerCreate200Response() {
  return {
    token: 'GmRhmhcxhwAzkoEqiMEg_DnyEysNkuNhszIySk9e',
    expiresIn: 60,
    transferUrl:
      'https://app.mxdraw.com/session-transfer?token=***&redirect=/member-center',
  };
}

export function getSessionTransferControllerConsume200Response() {
  return {
    accessToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    refreshToken: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...',
    user: {
      id: '123e4567-e89b-12d3-a456-426614174000',
      email: 'user@example.com',
      username: 'username',
      nickname: '用户昵称',
      avatar: 'https://example.com/avatar.jpg',
      role: {
        id: 'clxxxxxxx',
        name: 'USER',
        description: '普通用户，基础权限',
        isSystem: true,
        permissions: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            permission: faker.lorem.words(),
          }));
        })(),
      },
      status: 'ACTIVE',
      phone: '13800138000',
      phoneVerified: faker.datatype.boolean(),
      wechatId: 'oXYZ123...',
      provider: 'LOCAL',
      hasPassword: true,
      membershipTierLevel: faker.number.int(),
      membershipExpiresAt: faker.lorem.words(),
      membershipTier: 'VIP0',
      isVip: faker.datatype.boolean(),
    },
    restored: faker.datatype.boolean(),
    mfaSetupRequired: faker.datatype.boolean(),
    passwordChangeRequired: faker.helpers.arrayElement([
      'first_login',
      'expired',
    ]),
    passwordExpiringSoon: faker.datatype.boolean(),
  };
}

export function getUsersControllerCreate201Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.date.anytime().toISOString(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerFindAll200Response() {
  return {
    users: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        email: faker.internet.email(),
        username: faker.person.fullName(),
        nickname: faker.person.fullName(),
        avatar: faker.lorem.words(),
        phone: faker.lorem.words(),
        phoneVerified: faker.datatype.boolean(),
        wechatId: faker.string.uuid(),
        provider: 'LOCAL',
        status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
        role: {
          id: faker.string.uuid(),
          name: faker.person.fullName(),
          description: faker.lorem.words(),
          isSystem: faker.datatype.boolean(),
        },
        hasPassword: faker.datatype.boolean(),
        createdAt: faker.date.anytime().toISOString(),
        membershipTierLevel: faker.number.int(),
        membershipExpiresAt: faker.date.anytime().toISOString(),
        membershipTier: 'VIP0',
        isVip: faker.datatype.boolean(),
        updatedAt: faker.date.anytime().toISOString(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getUsersControllerSearchByEmail200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
  };
}

export function getUsersControllerSearchUsers200Response() {
  return {
    users: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        email: faker.internet.email(),
        username: faker.person.fullName(),
        nickname: faker.person.fullName(),
        avatar: faker.lorem.words(),
        phone: faker.lorem.words(),
        phoneVerified: faker.datatype.boolean(),
        wechatId: faker.string.uuid(),
        provider: 'LOCAL',
        status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
        role: {
          id: faker.string.uuid(),
          name: faker.person.fullName(),
          description: faker.lorem.words(),
          isSystem: faker.datatype.boolean(),
        },
        hasPassword: faker.datatype.boolean(),
        createdAt: faker.date.anytime().toISOString(),
        membershipTierLevel: faker.number.int(),
        membershipExpiresAt: faker.date.anytime().toISOString(),
        membershipTier: 'VIP0',
        isVip: faker.datatype.boolean(),
        updatedAt: faker.date.anytime().toISOString(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getUsersControllerGetProfile200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.lorem.words(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerUpdateProfile200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.lorem.words(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerGetDashboardStats200Response() {
  return {
    projectCount: faker.number.int(),
    totalFiles: faker.number.int(),
    todayUploads: faker.number.int(),
    fileTypeStats: {
      dwg: faker.number.int(),
      dxf: faker.number.int(),
      other: faker.number.int(),
    },
    storage: {
      used: faker.number.int(),
      total: faker.number.int(),
      remaining: faker.number.int(),
      usagePercent: faker.number.int(),
    },
  };
}

export function getUsersControllerUploadAvatar200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.lorem.words(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerUploadUserAvatar200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.lorem.words(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerFindOne200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.date.anytime().toISOString(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerUpdate200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.date.anytime().toISOString(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerUpdateStatus200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.date.anytime().toISOString(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerUpdateMembership200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    phone: faker.lorem.words(),
    phoneVerified: faker.datatype.boolean(),
    wechatId: faker.string.uuid(),
    provider: 'LOCAL',
    status: faker.helpers.arrayElement(['ACTIVE', 'INACTIVE', 'SUSPENDED']),
    role: {
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
    },
    hasPassword: faker.datatype.boolean(),
    createdAt: faker.date.anytime().toISOString(),
    membershipTierLevel: faker.number.int(),
    membershipExpiresAt: faker.date.anytime().toISOString(),
    membershipTier: 'VIP0',
    isVip: faker.datatype.boolean(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getUsersControllerDeactivateAccount200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getUsersControllerRestoreAccount200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getUsersControllerChangePassword200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getBillingControllerCreateOrder201Response() {
  return {
    id: faker.string.uuid(),
    orderNo: faker.lorem.words(),
    vipTierId: faker.string.uuid(),
    months: faker.number.int(),
    amount: faker.number.int(),
    status: 'PENDING',
    gateway: 'mock',
    gatewayOrderId: faker.string.uuid(),
    codeUrl: faker.internet.url(),
    payParams: {},
    redirectUrl: faker.internet.url(),
    vipTierName: 'VIP1',
    durationLabel: '3个月',
    priceYuan: faker.number.int(),
    createdAt: faker.date.anytime().toISOString(),
  };
}

export function getBillingControllerAutoCreateOrder201Response() {
  return {
    id: faker.string.uuid(),
    orderNo: faker.lorem.words(),
    vipTierId: faker.string.uuid(),
    months: faker.number.int(),
    amount: faker.number.int(),
    status: 'PENDING',
    gateway: 'mock',
    gatewayOrderId: faker.string.uuid(),
    codeUrl: faker.internet.url(),
    payParams: {},
    redirectUrl: faker.internet.url(),
    vipTierName: 'VIP1',
    durationLabel: '3个月',
    priceYuan: faker.number.int(),
    createdAt: faker.date.anytime().toISOString(),
  };
}

export function getBillingControllerRepayOrder201Response() {
  return {
    id: faker.string.uuid(),
    orderNo: faker.lorem.words(),
    vipTierId: faker.string.uuid(),
    months: faker.number.int(),
    amount: faker.number.int(),
    status: 'PENDING',
    gateway: 'mock',
    gatewayOrderId: faker.string.uuid(),
    codeUrl: faker.internet.url(),
    payParams: {},
    redirectUrl: faker.internet.url(),
    vipTierName: 'VIP1',
    durationLabel: '3个月',
    priceYuan: faker.number.int(),
    createdAt: faker.date.anytime().toISOString(),
  };
}

export function getBillingAdminControllerRefund201Response() {
  return {
    success: faker.datatype.boolean(),
  };
}

export function getBillingAdminControllerApproveRefundApplication201Response() {
  return {
    success: faker.datatype.boolean(),
  };
}

export function getBillingAdminControllerRejectRefundApplication201Response() {
  return {
    success: faker.datatype.boolean(),
  };
}

export function getBillingAdminControllerManualComplete201Response() {
  return {
    success: faker.datatype.boolean(),
  };
}

export function getDeviceAuthControllerRequestDeviceCode200Response() {
  return {
    device_code: faker.lorem.words(),
    user_code: 'HDKF-QLMD',
    verification_uri: 'https://example.com/device',
    verification_uri_complete: 'https://example.com/device?user_code=HDKF-QLMD',
    expires_in: 300,
    interval: 5,
  };
}

export function getDeviceAuthControllerAuthorizeDevice200Response() {
  return {
    success: true,
  };
}

export function getDeviceAuthControllerPollForToken200Response() {
  return {
    access_token: 'eyJhbGciOiJSUzI1NiIs...',
    token_type: 'Bearer',
    expires_in: 604800,
    refresh_token: 'eyJhbGciOiJIUzI1NiIs...',
    scope: 'vip',
  };
}

export function getIpWhitelistControllerList200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        ip: '203.0.113.0/24',
        source: 'manual',
        reason: faker.lorem.words(),
        createdBy: faker.lorem.words(),
        createdAt: faker.date.anytime().toISOString(),
        expiresAt: {},
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    pageSize: faker.number.int(),
  };
}

export function getIpWhitelistControllerCreate201Response() {
  return {
    id: faker.string.uuid(),
    ip: '203.0.113.0/24',
    source: 'manual',
    reason: faker.lorem.words(),
    createdBy: faker.lorem.words(),
    createdAt: faker.date.anytime().toISOString(),
    expiresAt: {},
  };
}

export function getIpBlacklistControllerList200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        ip: '203.0.113.0/24',
        source: 'manual',
        reason: faker.lorem.words(),
        createdBy: faker.lorem.words(),
        createdAt: faker.date.anytime().toISOString(),
        expiresAt: {},
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    pageSize: faker.number.int(),
  };
}

export function getIpBlacklistControllerCreate201Response() {
  return {
    id: faker.string.uuid(),
    ip: '203.0.113.0/24',
    source: 'manual',
    reason: faker.lorem.words(),
    createdBy: faker.lorem.words(),
    createdAt: faker.date.anytime().toISOString(),
    expiresAt: {},
  };
}

export function getSecurityAccessAttemptControllerList200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        ip: '203.0.113.7',
        firstSeen: faker.date.anytime().toISOString(),
        lastSeen: faker.date.anytime().toISOString(),
        count: faker.number.int(),
        reasons: { ip_not_allowed: 3 },
        account: {},
        userAgent: {},
        inWhitelist: faker.datatype.boolean(),
        inBlacklist: faker.datatype.boolean(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    pageSize: faker.number.int(),
  };
}

export function getUploadTokenControllerCreateUploadToken200Response() {
  return {
    token: faker.lorem.words(),
    path: faker.lorem.words(),
    operation: faker.helpers.arrayElement(['upload']),
    expiresAt: faker.date.anytime().toISOString(),
  };
}

export function getUserCleanupControllerGetStats200Response() {
  return {
    pendingCleanup: 15,
    expiryDate: '2026-03-11T00:00:00.000Z',
    delayDays: 30,
  };
}

export function getUserCleanupControllerTriggerCleanup200Response() {
  return {
    message: '清理完成: 处理 0 个用户',
    success: true,
    processedUsers: faker.number.int(),
    deletedMembers: faker.number.int(),
    deletedProjects: faker.number.int(),
    deletedAuditLogs: faker.number.int(),
    deletedPaymentOrders: faker.number.int(),
    deletedMemberships: faker.number.int(),
    deletedFileShares: faker.number.int(),
    deletedBatchJobs: faker.number.int(),
    markedForStorageCleanup: faker.number.int(),
    errors: [],
  };
}

export function getRolesControllerFindAll200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      category: faker.helpers.arrayElement(['SYSTEM', 'PROJECT', 'CUSTOM']),
      level: faker.number.int(),
      isSystem: faker.datatype.boolean(),
      permissions: ['system:user:read', 'system:role:read', 'system:font:read'],
      createdAt: faker.date.anytime().toISOString(),
      updatedAt: faker.date.anytime().toISOString(),
    }));
  })();
}

export function getRolesControllerCreate201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    category: faker.helpers.arrayElement(['SYSTEM', 'PROJECT', 'CUSTOM']),
    level: faker.number.int(),
    isSystem: faker.datatype.boolean(),
    permissions: ['system:user:read', 'system:role:read', 'system:font:read'],
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getRolesControllerFindOne200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    category: faker.helpers.arrayElement(['SYSTEM', 'PROJECT', 'CUSTOM']),
    level: faker.number.int(),
    isSystem: faker.datatype.boolean(),
    permissions: ['system:user:read', 'system:role:read', 'system:font:read'],
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getRolesControllerUpdate200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    category: faker.helpers.arrayElement(['SYSTEM', 'PROJECT', 'CUSTOM']),
    level: faker.number.int(),
    isSystem: faker.datatype.boolean(),
    permissions: ['system:user:read', 'system:role:read', 'system:font:read'],
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getRolesControllerGetRolePermissions200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => faker.lorem.words());
  })();
}

export function getRolesControllerAddPermissions200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    category: faker.helpers.arrayElement(['SYSTEM', 'PROJECT', 'CUSTOM']),
    level: faker.number.int(),
    isSystem: faker.datatype.boolean(),
    permissions: ['system:user:read', 'system:role:read', 'system:font:read'],
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getRolesControllerRemovePermissions200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    category: faker.helpers.arrayElement(['SYSTEM', 'PROJECT', 'CUSTOM']),
    level: faker.number.int(),
    isSystem: faker.datatype.boolean(),
    permissions: ['system:user:read', 'system:role:read', 'system:font:read'],
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getRolesControllerGetSystemProjectRoles200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.string.uuid(),
      projectId: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
      isOwnerRole: faker.datatype.boolean(),
      permissions: (() => {
        const arrayMin = 1;
        const arrayMax = MAX_ARRAY_LENGTH;
        const safeMin = Math.min(arrayMin, arrayMax);
        return [
          ...new Array(
            faker.number.int({ min: safeMin, max: arrayMax })
          ).keys(),
        ].map((_) => ({
          id: faker.string.uuid(),
          projectRoleId: faker.string.uuid(),
          permission: faker.helpers.arrayElement([
            'PROJECT_UPDATE',
            'PROJECT_DELETE',
            'PROJECT_MEMBER_MANAGE',
            'PROJECT_MEMBER_ASSIGN',
            'PROJECT_TRANSFER',
            'PROJECT_TRANSFER_MANAGE',
            'PROJECT_ROLE_MANAGE',
            'PROJECT_ROLE_PERMISSION_MANAGE',
            'FILE_CREATE',
            'FILE_UPLOAD',
            'FILE_OPEN',
            'FILE_EDIT',
            'FILE_DELETE',
            'FILE_TRASH_MANAGE',
            'FILE_DOWNLOAD',
            'FILE_SHARE',
            'FILE_MOVE',
            'FILE_COPY',
            'CAD_SAVE',
            'CAD_EXTERNAL_REFERENCE',
            'VERSION_READ',
          ]),
          createdAt: faker.date.anytime().toISOString(),
        }));
      })(),
      _count: {},
      project: {},
      createdAt: faker.date.anytime().toISOString(),
      updatedAt: faker.date.anytime().toISOString(),
    }));
  })();
}

export function getRolesControllerGetProjectRolesByProject200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.string.uuid(),
      projectId: faker.string.uuid(),
      name: faker.person.fullName(),
      description: faker.lorem.words(),
      isSystem: faker.datatype.boolean(),
      isOwnerRole: faker.datatype.boolean(),
      permissions: (() => {
        const arrayMin = 1;
        const arrayMax = MAX_ARRAY_LENGTH;
        const safeMin = Math.min(arrayMin, arrayMax);
        return [
          ...new Array(
            faker.number.int({ min: safeMin, max: arrayMax })
          ).keys(),
        ].map((_) => ({
          id: faker.string.uuid(),
          projectRoleId: faker.string.uuid(),
          permission: faker.helpers.arrayElement([
            'PROJECT_UPDATE',
            'PROJECT_DELETE',
            'PROJECT_MEMBER_MANAGE',
            'PROJECT_MEMBER_ASSIGN',
            'PROJECT_TRANSFER',
            'PROJECT_TRANSFER_MANAGE',
            'PROJECT_ROLE_MANAGE',
            'PROJECT_ROLE_PERMISSION_MANAGE',
            'FILE_CREATE',
            'FILE_UPLOAD',
            'FILE_OPEN',
            'FILE_EDIT',
            'FILE_DELETE',
            'FILE_TRASH_MANAGE',
            'FILE_DOWNLOAD',
            'FILE_SHARE',
            'FILE_MOVE',
            'FILE_COPY',
            'CAD_SAVE',
            'CAD_EXTERNAL_REFERENCE',
            'VERSION_READ',
          ]),
          createdAt: faker.date.anytime().toISOString(),
        }));
      })(),
      _count: {},
      project: {},
      createdAt: faker.date.anytime().toISOString(),
      updatedAt: faker.date.anytime().toISOString(),
    }));
  })();
}

export function getRolesControllerCreateProjectRole201Response() {
  return {
    id: faker.string.uuid(),
    projectId: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    isSystem: faker.datatype.boolean(),
    isOwnerRole: faker.datatype.boolean(),
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        projectRoleId: faker.string.uuid(),
        permission: faker.helpers.arrayElement([
          'PROJECT_UPDATE',
          'PROJECT_DELETE',
          'PROJECT_MEMBER_MANAGE',
          'PROJECT_MEMBER_ASSIGN',
          'PROJECT_TRANSFER',
          'PROJECT_TRANSFER_MANAGE',
          'PROJECT_ROLE_MANAGE',
          'PROJECT_ROLE_PERMISSION_MANAGE',
          'FILE_CREATE',
          'FILE_UPLOAD',
          'FILE_OPEN',
          'FILE_EDIT',
          'FILE_DELETE',
          'FILE_TRASH_MANAGE',
          'FILE_DOWNLOAD',
          'FILE_SHARE',
          'FILE_MOVE',
          'FILE_COPY',
          'CAD_SAVE',
          'CAD_EXTERNAL_REFERENCE',
          'VERSION_READ',
        ]),
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    _count: {},
    project: {},
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getRolesControllerUpdateProjectRole200Response() {
  return {
    id: faker.string.uuid(),
    projectId: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    isSystem: faker.datatype.boolean(),
    isOwnerRole: faker.datatype.boolean(),
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        projectRoleId: faker.string.uuid(),
        permission: faker.helpers.arrayElement([
          'PROJECT_UPDATE',
          'PROJECT_DELETE',
          'PROJECT_MEMBER_MANAGE',
          'PROJECT_MEMBER_ASSIGN',
          'PROJECT_TRANSFER',
          'PROJECT_TRANSFER_MANAGE',
          'PROJECT_ROLE_MANAGE',
          'PROJECT_ROLE_PERMISSION_MANAGE',
          'FILE_CREATE',
          'FILE_UPLOAD',
          'FILE_OPEN',
          'FILE_EDIT',
          'FILE_DELETE',
          'FILE_TRASH_MANAGE',
          'FILE_DOWNLOAD',
          'FILE_SHARE',
          'FILE_MOVE',
          'FILE_COPY',
          'CAD_SAVE',
          'CAD_EXTERNAL_REFERENCE',
          'VERSION_READ',
        ]),
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    _count: {},
    project: {},
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getProjectRolesControllerCreateProjectRole201Response() {
  return {
    id: faker.string.uuid(),
    projectId: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    isSystem: faker.datatype.boolean(),
    isOwnerRole: faker.datatype.boolean(),
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        projectRoleId: faker.string.uuid(),
        permission: faker.helpers.arrayElement([
          'PROJECT_UPDATE',
          'PROJECT_DELETE',
          'PROJECT_MEMBER_MANAGE',
          'PROJECT_MEMBER_ASSIGN',
          'PROJECT_TRANSFER',
          'PROJECT_TRANSFER_MANAGE',
          'PROJECT_ROLE_MANAGE',
          'PROJECT_ROLE_PERMISSION_MANAGE',
          'FILE_CREATE',
          'FILE_UPLOAD',
          'FILE_OPEN',
          'FILE_EDIT',
          'FILE_DELETE',
          'FILE_TRASH_MANAGE',
          'FILE_DOWNLOAD',
          'FILE_SHARE',
          'FILE_MOVE',
          'FILE_COPY',
          'CAD_SAVE',
          'CAD_EXTERNAL_REFERENCE',
          'VERSION_READ',
        ]),
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    _count: {},
    project: {},
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getProjectRolesControllerUpdateProjectRole200Response() {
  return {
    id: faker.string.uuid(),
    projectId: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    isSystem: faker.datatype.boolean(),
    isOwnerRole: faker.datatype.boolean(),
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        projectRoleId: faker.string.uuid(),
        permission: faker.helpers.arrayElement([
          'PROJECT_UPDATE',
          'PROJECT_DELETE',
          'PROJECT_MEMBER_MANAGE',
          'PROJECT_MEMBER_ASSIGN',
          'PROJECT_TRANSFER',
          'PROJECT_TRANSFER_MANAGE',
          'PROJECT_ROLE_MANAGE',
          'PROJECT_ROLE_PERMISSION_MANAGE',
          'FILE_CREATE',
          'FILE_UPLOAD',
          'FILE_OPEN',
          'FILE_EDIT',
          'FILE_DELETE',
          'FILE_TRASH_MANAGE',
          'FILE_DOWNLOAD',
          'FILE_SHARE',
          'FILE_MOVE',
          'FILE_COPY',
          'CAD_SAVE',
          'CAD_EXTERNAL_REFERENCE',
          'VERSION_READ',
        ]),
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    _count: {},
    project: {},
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getProjectRolesControllerAddProjectRolePermissions200Response() {
  return {
    id: faker.string.uuid(),
    projectId: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    isSystem: faker.datatype.boolean(),
    isOwnerRole: faker.datatype.boolean(),
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        projectRoleId: faker.string.uuid(),
        permission: faker.helpers.arrayElement([
          'PROJECT_UPDATE',
          'PROJECT_DELETE',
          'PROJECT_MEMBER_MANAGE',
          'PROJECT_MEMBER_ASSIGN',
          'PROJECT_TRANSFER',
          'PROJECT_TRANSFER_MANAGE',
          'PROJECT_ROLE_MANAGE',
          'PROJECT_ROLE_PERMISSION_MANAGE',
          'FILE_CREATE',
          'FILE_UPLOAD',
          'FILE_OPEN',
          'FILE_EDIT',
          'FILE_DELETE',
          'FILE_TRASH_MANAGE',
          'FILE_DOWNLOAD',
          'FILE_SHARE',
          'FILE_MOVE',
          'FILE_COPY',
          'CAD_SAVE',
          'CAD_EXTERNAL_REFERENCE',
          'VERSION_READ',
        ]),
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    _count: {},
    project: {},
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getProjectRolesControllerRemoveProjectRolePermissions200Response() {
  return {
    id: faker.string.uuid(),
    projectId: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    isSystem: faker.datatype.boolean(),
    isOwnerRole: faker.datatype.boolean(),
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        projectRoleId: faker.string.uuid(),
        permission: faker.helpers.arrayElement([
          'PROJECT_UPDATE',
          'PROJECT_DELETE',
          'PROJECT_MEMBER_MANAGE',
          'PROJECT_MEMBER_ASSIGN',
          'PROJECT_TRANSFER',
          'PROJECT_TRANSFER_MANAGE',
          'PROJECT_ROLE_MANAGE',
          'PROJECT_ROLE_PERMISSION_MANAGE',
          'FILE_CREATE',
          'FILE_UPLOAD',
          'FILE_OPEN',
          'FILE_EDIT',
          'FILE_DELETE',
          'FILE_TRASH_MANAGE',
          'FILE_DOWNLOAD',
          'FILE_SHARE',
          'FILE_MOVE',
          'FILE_COPY',
          'CAD_SAVE',
          'CAD_EXTERNAL_REFERENCE',
          'VERSION_READ',
        ]),
        createdAt: faker.date.anytime().toISOString(),
      }));
    })(),
    _count: {},
    project: {},
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getProjectControllerCreateProject201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    status: faker.helpers.arrayElement(['ACTIVE', 'ARCHIVED', 'DELETED']),
    ownerId: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    memberCount: faker.number.int(),
    transferOutToProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
  };
}

export function getProjectControllerGetProjects200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getProjectControllerGetDeletedProjects200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getProjectControllerGetPersonalSpace200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getProjectControllerGetProject200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    status: faker.helpers.arrayElement(['ACTIVE', 'ARCHIVED', 'DELETED']),
    ownerId: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    memberCount: faker.number.int(),
    transferOutToProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
  };
}

export function getProjectControllerUpdateProject200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    status: faker.helpers.arrayElement(['ACTIVE', 'ARCHIVED', 'DELETED']),
    ownerId: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    memberCount: faker.number.int(),
    transferOutToProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
  };
}

export function getProjectControllerDeleteProject200Response() {
  return {
    message: faker.lorem.words(),
    nodeId: faker.string.uuid(),
    success: faker.datatype.boolean(),
  };
}

export function getProjectControllerUpdateTransferSettings200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    status: faker.helpers.arrayElement(['ACTIVE', 'ARCHIVED', 'DELETED']),
    ownerId: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    memberCount: faker.number.int(),
    transferOutToProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferOutToLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromProject: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromPersonalSpace: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
    transferInFromLibrary: faker.helpers.arrayElement([
      'NONE',
      'COPY_ONLY',
      'MOVE_ONLY',
      'ALL',
    ]),
  };
}

export function getProjectControllerGetStorageQuota200Response() {
  return {
    used: faker.number.int(),
    total: faker.number.int(),
    remaining: faker.number.int(),
    usagePercent: faker.number.int(),
  };
}

export function getProjectControllerGetProjectQuota200Response() {
  return {
    projectId: faker.string.uuid(),
    used: faker.number.int(),
    limit: faker.number.int(),
    remaining: faker.number.int(),
  };
}

export function getNodeControllerCreateNode201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerCreateFolder201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerCreateDrawing201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerGetRootNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerGetParentContext200Response() {
  return {
    parentId: faker.string.uuid(),
    pageNumber: faker.number.int(),
    positionInPage: faker.number.int(),
    totalPages: faker.number.int(),
    total: faker.number.int(),
  };
}

export function getNodeControllerGetNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
    children: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    ancestors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({}));
    })(),
  };
}

export function getNodeControllerUpdateNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerDeleteNode200Response() {
  return {
    message: faker.lorem.words(),
    nodeId: faker.string.uuid(),
    success: faker.datatype.boolean(),
  };
}

export function getNodeControllerGetChildren200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getNodeControllerMoveNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerCopyNode201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerRestoreNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getNodeControllerBatchDeleteNodes200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getNodeControllerBatchMoveNodes200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getNodeControllerBatchCopyNodes201Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getNodeControllerLookupNodes200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.string.uuid(),
      parentId: {},
      name: faker.person.fullName(),
      nodeType: faker.lorem.words(),
    }));
  })();
}

export function getNodeControllerSearch200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getNodeControllerResolvePath200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getTrashControllerGetTrash200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getTrashControllerClearTrash200Response() {
  return {
    message: faker.lorem.words(),
    nodeId: faker.string.uuid(),
    success: faker.datatype.boolean(),
  };
}

export function getTrashControllerRestoreTrashItems200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getTrashControllerPermanentlyDeleteTrashItems200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getTrashControllerGetProjectTrash200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getTrashControllerClearProjectTrash200Response() {
  return {
    message: faker.lorem.words(),
    nodeId: faker.string.uuid(),
    success: faker.datatype.boolean(),
  };
}

export function getMemberControllerGetProjectMembers200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.string.uuid(),
      email: faker.internet.email(),
      username: faker.person.fullName(),
      nickname: faker.person.fullName(),
      avatar: faker.lorem.words(),
      projectRoleId: faker.string.uuid(),
      projectRoleName: faker.person.fullName(),
      joinedAt: faker.date.anytime().toISOString(),
    }));
  })();
}

export function getMemberControllerAddProjectMember201Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    projectRoleId: faker.string.uuid(),
    projectRoleName: faker.person.fullName(),
    joinedAt: faker.date.anytime().toISOString(),
  };
}

export function getMemberControllerUpdateProjectMember200Response() {
  return {
    id: faker.string.uuid(),
    email: faker.internet.email(),
    username: faker.person.fullName(),
    nickname: faker.person.fullName(),
    avatar: faker.lorem.words(),
    projectRoleId: faker.string.uuid(),
    projectRoleName: faker.person.fullName(),
    joinedAt: faker.date.anytime().toISOString(),
  };
}

export function getMemberControllerRemoveProjectMember200Response() {
  return {
    message: faker.lorem.words(),
    nodeId: faker.string.uuid(),
    success: faker.datatype.boolean(),
  };
}

export function getMemberControllerGetUserProjectPermissions200Response() {
  return {
    projectId: faker.string.uuid(),
    userId: faker.string.uuid(),
    role: faker.lorem.words(),
    permissions: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getVersionControlControllerGetFileHistory200Response() {
  return {
    success: true,
    message: '获取成功',
    entries: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        revision: 123,
        author: 'user@example.com',
        date: '2026-03-03T10:30:00.000Z',
        message: 'Update drawing file',
        userName: '张三',
        paths: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => ({
            action: 'A',
            kind: 'file',
            path: '/path/to/file.dwg',
          }));
        })(),
      }));
    })(),
    totalCount: 23,
  };
}

export function getVersionControlControllerGetFileContentAtRevision200Response() {
  return {
    success: true,
    message: '获取成功',
    content: faker.lorem.words(),
  };
}

export function getVersionControlControllerListDirectoryAtRevision200Response() {
  return {
    success: faker.datatype.boolean(),
    message: faker.lorem.words(),
    files: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getFontsControllerBatchDeleteFonts200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getThumbnailControllerCheckThumbnail200Response() {
  return {
    code: faker.number.int(),
    message: 'ok',
    exists: faker.datatype.boolean(),
  };
}

export function getThumbnailControllerUploadThumbnail200Response() {
  return {
    code: faker.number.int(),
    message: '缩略图上传成功',
    data: {
      fileName: faker.person.fullName(),
    },
  };
}

export function getConversionStatusControllerGetConversionStatusdefaultResponse() {
  return {
    fileStatus: 'PROCESSING',
    taskId: faker.string.uuid(),
    taskStatus: faker.lorem.words(),
    error: faker.lorem.words(),
  };
}

export function getConversionTaskControllerSubmitTaskdefaultResponse() {
  return {
    taskId: faker.string.uuid(),
    nodeId: faker.string.uuid(),
    async: true,
  };
}

export function getConversionTaskControllerListTasksdefaultResponse() {
  return {
    tasks: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        nodeId: faker.string.uuid(),
        name: faker.person.fullName(),
        fileStatus: 'PROCESSING',
        taskId: faker.string.uuid(),
        taskStatus: faker.lorem.words(),
        progress: 42,
        error: faker.lorem.words(),
        errorCategory: faker.helpers.arrayElement([
          'timeout',
          'killed',
          'not-started',
          'output-unparseable',
          'content-error',
          'unknown',
        ]),
        queuePosition: 2,
        updatedAt: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
  };
}

export function getConversionTaskControllerListHistorydefaultResponse() {
  return {
    tasks: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        nodeId: faker.string.uuid(),
        name: faker.person.fullName(),
        fileStatus: 'PROCESSING',
        taskId: faker.string.uuid(),
        taskStatus: faker.lorem.words(),
        progress: 42,
        error: faker.lorem.words(),
        errorCategory: faker.helpers.arrayElement([
          'timeout',
          'killed',
          'not-started',
          'output-unparseable',
          'content-error',
          'unknown',
        ]),
        queuePosition: 2,
        updatedAt: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    hasMore: faker.datatype.boolean(),
  };
}

export function getConversionTaskControllerGetQuotadefaultResponse() {
  return {
    limit: faker.number.int(),
    used: faker.number.int(),
    remaining: faker.number.int(),
    windowHours: faker.number.int(),
    unlimited: faker.datatype.boolean(),
    resetsAt: {},
    scope: faker.lorem.words(),
  };
}

export function getSaveControllerSaveMxwebToNode200Response() {
  return {
    nodeId: faker.string.uuid(),
    path: faker.lorem.words(),
  };
}

export function getSaveControllerSaveMxwebAs200Response() {
  return {
    success: faker.datatype.boolean(),
    message: faker.lorem.words(),
    nodeId: faker.string.uuid(),
    fileName: faker.person.fullName(),
    path: faker.lorem.words(),
    projectId: faker.string.uuid(),
    parentId: faker.string.uuid(),
  };
}

export function getMxcadExternalRefControllerGetPreloadingData200Response() {
  return {
    tz: faker.datatype.boolean(),
    src_file_md5: faker.lorem.words(),
    images: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: faker.person.fullName(),
        size: faker.number.int(),
        type: faker.lorem.words(),
      }));
    })(),
    externalReference: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: faker.person.fullName(),
        size: faker.number.int(),
        type: faker.lorem.words(),
      }));
    })(),
  };
}

export function getMxcadExternalRefControllerCheckExternalReference200Response() {
  return {
    exists: faker.datatype.boolean(),
  };
}

export function getMxcadExternalRefControllerRefreshExternalReferences200Response() {
  return {
    code: faker.number.int(),
    message: '刷新成功',
    stats: {
      added: faker.number.int(),
      updated: faker.number.int(),
      removed: faker.number.int(),
    },
  };
}

export function getMxcadUploadControllerCheckChunkExist200Response() {
  return {
    exists: true,
  };
}

export function getMxcadUploadControllerCheckFileExist200Response() {
  return {
    exists: faker.datatype.boolean(),
    nodeId: faker.string.uuid(),
  };
}

export function getMxcadUploadControllerUploadFile200Response() {
  return {
    nodeId: faker.string.uuid(),
    tz: faker.datatype.boolean(),
    ret: faker.lorem.words(),
  };
}

export function getShareControllerCreateShare201Response() {
  return {
    token: faker.lorem.words(),
    url: faker.internet.url(),
    expiresAt: {},
  };
}

export function getShareControllerListShares200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        token: faker.lorem.words(),
        url: faker.internet.url(),
        fileId: faker.string.uuid(),
        fileName: faker.person.fullName(),
        expiresAt: {},
        usedCount: faker.number.int(),
        createdAt: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    pageSize: faker.number.int(),
  };
}

export function getShareControllerResolveShare200Response() {
  return {
    fileId: faker.string.uuid(),
  };
}

export function getShareControllerResolveShareNode200Response() {
  return {
    name: faker.person.fullName(),
    fileHash: faker.lorem.words(),
    path: faker.lorem.words(),
    parentId: faker.string.uuid(),
    id: faker.string.uuid(),
    deletedAt: faker.lorem.words(),
    updatedAt: faker.lorem.words(),
  };
}

export function getShareControllerGetFileShares200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      token: faker.lorem.words(),
      url: faker.internet.url(),
      fileName: faker.person.fullName(),
      expiresAt: {},
      createdAt: faker.lorem.words(),
      createdBy: faker.lorem.words(),
    }));
  })();
}

export function getAdminControllerGetOverview200Response() {
  return {
    totalUsers: 1234,
    paidUsers: 56,
  };
}

export function getAdminControllerGetRegistrationStats200Response() {
  return {
    startDate: '2026-07-27',
    endDate: '2026-08-25',
    series: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        date: '2026-08-25',
        count: 12,
      }));
    })(),
    total: 356,
  };
}

export function getAdminControllerGetPurchaseStats200Response() {
  return {
    startDate: '2026-07-27',
    endDate: '2026-08-25',
    series: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        date: '2026-08-25',
        orderCount: 5,
        userCount: 4,
        amount: 199800,
      }));
    })(),
    totals: {
      orderCount: 128,
      userCount: 96,
      amount: 12798000,
      refundedCount: 2,
    },
    byTier: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        tierId: faker.string.uuid(),
        tierLevel: 1,
        tierName: faker.person.fullName(),
        orderCount: 10,
        userCount: 8,
        amount: 1598000,
      }));
    })(),
  };
}

export function getAdminControllerGetCacheStats200Response() {
  return {
    message: faker.lorem.words(),
    data: {
      size: faker.number.int(),
      hits: faker.number.int(),
      misses: faker.number.int(),
      hitRate: faker.number.int(),
    },
  };
}

export function getAdminControllerCleanupCache200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getAdminControllerClearUserCache200Response() {
  return {
    message: faker.lorem.words(),
  };
}

export function getHealthControllerCheckFull200Response() {
  return {
    status: 'ok',
    info: { database: { status: 'up' } },
    error: {},
    details: { database: { status: 'up' } },
  };
}

export function getHealthControllerCheckFull503Response() {
  return {
    status: 'error',
    info: { database: { status: 'up' } },
    error: { redis: { status: 'down', message: 'Could not connect' } },
    details: {
      database: { status: 'up' },
      redis: { status: 'down', message: 'Could not connect' },
    },
  };
}

export function getHealthControllerCheck200Response() {
  return {
    status: 'ok',
    info: { database: { status: 'up' } },
    error: {},
    details: { database: { status: 'up' } },
  };
}

export function getHealthControllerCheck503Response() {
  return {
    status: 'error',
    info: { database: { status: 'up' } },
    error: { redis: { status: 'down', message: 'Could not connect' } },
    details: {
      database: { status: 'up' },
      redis: { status: 'down', message: 'Could not connect' },
    },
  };
}

export function getQueueControllerGetQueueStats200Response() {
  return faker.helpers.arrayElement([
    {
      queueLength: faker.number.int(),
      criticalPriorityQueueLength: faker.number.int(),
      highPriorityQueueLength: faker.number.int(),
      lowPriorityQueueLength: faker.number.int(),
      runningCount: faker.number.int(),
      maxConcurrent: faker.number.int(),
      timeout: faker.number.int(),
    },
    null,
  ]);
}

export function getNoticeCenterControllerGetCurrent200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.string.uuid(),
      kind: faker.lorem.words(),
      level: faker.lorem.words(),
      title: faker.lorem.words(),
      body: faker.lorem.words(),
      userId: faker.string.uuid(),
      startAt: faker.date.anytime().toISOString(),
      endAt: faker.date.anytime().toISOString(),
      autoExpire: faker.datatype.boolean(),
      publishedAt: faker.date.anytime().toISOString(),
      notifiedAt: faker.date.anytime().toISOString(),
      retractedAt: faker.date.anytime().toISOString(),
      publishedById: faker.string.uuid(),
      createdAt: faker.date.anytime().toISOString(),
      updatedAt: faker.date.anytime().toISOString(),
    }));
  })();
}

export function getNoticeCenterControllerIssueTicket200Response() {
  return {
    ticket: faker.lorem.words(),
  };
}

export function getNoticeCenterControllerListAll200Response() {
  return (() => {
    const arrayMin = 1;
    const arrayMax = MAX_ARRAY_LENGTH;
    const safeMin = Math.min(arrayMin, arrayMax);
    return [
      ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
    ].map((_) => ({
      id: faker.string.uuid(),
      kind: faker.lorem.words(),
      level: faker.lorem.words(),
      title: faker.lorem.words(),
      body: faker.lorem.words(),
      userId: faker.string.uuid(),
      startAt: faker.date.anytime().toISOString(),
      endAt: faker.date.anytime().toISOString(),
      autoExpire: faker.datatype.boolean(),
      publishedAt: faker.date.anytime().toISOString(),
      notifiedAt: faker.date.anytime().toISOString(),
      retractedAt: faker.date.anytime().toISOString(),
      publishedById: faker.string.uuid(),
      createdAt: faker.date.anytime().toISOString(),
      updatedAt: faker.date.anytime().toISOString(),
    }));
  })();
}

export function getNoticeCenterControllerCreate201Response() {
  return {
    id: faker.string.uuid(),
    kind: faker.lorem.words(),
    level: faker.lorem.words(),
    title: faker.lorem.words(),
    body: faker.lorem.words(),
    userId: faker.string.uuid(),
    startAt: faker.date.anytime().toISOString(),
    endAt: faker.date.anytime().toISOString(),
    autoExpire: faker.datatype.boolean(),
    publishedAt: faker.date.anytime().toISOString(),
    notifiedAt: faker.date.anytime().toISOString(),
    retractedAt: faker.date.anytime().toISOString(),
    publishedById: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getNoticeCenterControllerPublish200Response() {
  return {
    id: faker.string.uuid(),
    kind: faker.lorem.words(),
    level: faker.lorem.words(),
    title: faker.lorem.words(),
    body: faker.lorem.words(),
    userId: faker.string.uuid(),
    startAt: faker.date.anytime().toISOString(),
    endAt: faker.date.anytime().toISOString(),
    autoExpire: faker.datatype.boolean(),
    publishedAt: faker.date.anytime().toISOString(),
    notifiedAt: faker.date.anytime().toISOString(),
    retractedAt: faker.date.anytime().toISOString(),
    publishedById: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getNoticeCenterControllerUpdate200Response() {
  return {
    id: faker.string.uuid(),
    kind: faker.lorem.words(),
    level: faker.lorem.words(),
    title: faker.lorem.words(),
    body: faker.lorem.words(),
    userId: faker.string.uuid(),
    startAt: faker.date.anytime().toISOString(),
    endAt: faker.date.anytime().toISOString(),
    autoExpire: faker.datatype.boolean(),
    publishedAt: faker.date.anytime().toISOString(),
    notifiedAt: faker.date.anytime().toISOString(),
    retractedAt: faker.date.anytime().toISOString(),
    publishedById: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getNoticeCenterControllerRetract200Response() {
  return {
    id: faker.string.uuid(),
    kind: faker.lorem.words(),
    level: faker.lorem.words(),
    title: faker.lorem.words(),
    body: faker.lorem.words(),
    userId: faker.string.uuid(),
    startAt: faker.date.anytime().toISOString(),
    endAt: faker.date.anytime().toISOString(),
    autoExpire: faker.datatype.boolean(),
    publishedAt: faker.date.anytime().toISOString(),
    notifiedAt: faker.date.anytime().toISOString(),
    retractedAt: faker.date.anytime().toISOString(),
    publishedById: faker.string.uuid(),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
  };
}

export function getPublicFileControllerAccessFile200Response() {
  return null;
}

export function getPublicFileControllerAccessFileByHashPattern200Response() {
  return null;
}

export function getPublicFileControllerUploadExtReference200Response() {
  return {
    ret: 'ok',
    hash: faker.lorem.words(),
    message: faker.lorem.words(),
  };
}

export function getPublicFileControllerCheckExtReference200Response() {
  return {
    exists: faker.datatype.boolean(),
  };
}

export function getPublicFileControllerGetPreloadingData200Response() {
  return {
    tz: faker.datatype.boolean(),
    src_file_md5: faker.lorem.words(),
    images: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: faker.person.fullName(),
        size: faker.number.int(),
        type: faker.lorem.words(),
      }));
    })(),
    externalReference: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: faker.person.fullName(),
        size: faker.number.int(),
        type: faker.lorem.words(),
      }));
    })(),
  };
}

export function getLibraryControllerGetDrawingLibrary200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerGetDrawingChildren200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getLibraryControllerGetDrawingAllFiles200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getLibraryControllerGetDrawingFile200Response() {
  return null;
}

export function getLibraryControllerGetDrawingNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerRenameDrawingNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerDownloadDrawingNode200Response() {
  return {
    success: true,
    message: '获取成功',
    content: faker.lorem.words(),
  };
}

export function getLibraryControllerResolveDrawingPath200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerCreateDrawingFolder201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerMoveDrawingNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerCopyDrawingNode201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerBatchDeleteDrawingNodes200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getLibraryControllerBatchMoveDrawingNodes200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getLibraryControllerBatchCopyDrawingNodes201Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getLibraryControllerGetBlockLibrary200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerGetBlockChildren200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getLibraryControllerGetBlockAllFiles200Response() {
  return {
    nodes: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        name: faker.person.fullName(),
        description: faker.lorem.words(),
        nodeType: faker.helpers.arrayElement([
          'FILE',
          'FOLDER',
          'PROJECT',
          'PERSONAL_SPACE',
          'LIBRARY_DRAWING',
          'LIBRARY_BLOCK',
        ]),
        isFolder: faker.datatype.boolean(),
        isRoot: faker.datatype.boolean(),
        parentId: faker.string.uuid(),
        path: faker.lorem.words(),
        size: faker.number.int(),
        mimeType: faker.lorem.words(),
        fileHash: faker.lorem.words(),
        fileStatus: faker.helpers.arrayElement([
          'UPLOADING',
          'PROCESSING',
          'COMPLETED',
          'FAILED',
          'DELETED',
        ]),
        createdAt: faker.date.anytime().toISOString(),
        updatedAt: faker.date.anytime().toISOString(),
        deletedAt: faker.date.anytime().toISOString(),
        ownerId: faker.string.uuid(),
        personalSpaceKey: faker.lorem.words(),
        libraryKey: faker.lorem.words(),
        childrenCount: faker.number.int(),
        memberCount: faker.number.int(),
        projectId: faker.string.uuid(),
        childrenCountTrash: faker.number.int(),
        ancestorPath: faker.lorem.words(),
        sourceType: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
    page: faker.number.int(),
    limit: faker.number.int(),
    totalPages: faker.number.int(),
  };
}

export function getLibraryControllerResolveBlockPath200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerGetBlockFile200Response() {
  return null;
}

export function getLibraryControllerGetBlockNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerRenameBlockNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerDownloadBlockNode200Response() {
  return {
    success: true,
    message: '获取成功',
    content: faker.lorem.words(),
  };
}

export function getLibraryControllerCreateBlockFolder201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerMoveBlockNode200Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerCopyBlockNode201Response() {
  return {
    id: faker.string.uuid(),
    name: faker.person.fullName(),
    description: faker.lorem.words(),
    nodeType: faker.helpers.arrayElement([
      'FILE',
      'FOLDER',
      'PROJECT',
      'PERSONAL_SPACE',
      'LIBRARY_DRAWING',
      'LIBRARY_BLOCK',
    ]),
    isFolder: faker.datatype.boolean(),
    isRoot: faker.datatype.boolean(),
    parentId: faker.string.uuid(),
    path: faker.lorem.words(),
    size: faker.number.int(),
    mimeType: faker.lorem.words(),
    fileHash: faker.lorem.words(),
    fileStatus: faker.helpers.arrayElement([
      'UPLOADING',
      'PROCESSING',
      'COMPLETED',
      'FAILED',
      'DELETED',
    ]),
    createdAt: faker.date.anytime().toISOString(),
    updatedAt: faker.date.anytime().toISOString(),
    deletedAt: faker.date.anytime().toISOString(),
    ownerId: faker.string.uuid(),
    personalSpaceKey: faker.lorem.words(),
    libraryKey: faker.lorem.words(),
    childrenCount: faker.number.int(),
    memberCount: faker.number.int(),
    projectId: faker.string.uuid(),
    childrenCountTrash: faker.number.int(),
    ancestorPath: faker.lorem.words(),
    sourceType: faker.lorem.words(),
  };
}

export function getLibraryControllerBatchDeleteBlockNodes200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getLibraryControllerBatchMoveBlockNodes200Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getLibraryControllerBatchCopyBlockNodes201Response() {
  return {
    successCount: faker.number.int(),
    failedCount: faker.number.int(),
    successIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    failedIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    createdIds: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getBatchDownloadControllerGetProgressdefaultResponse() {
  return {
    taskId: faker.string.uuid(),
    status: faker.lorem.words(),
    mode: faker.helpers.arrayElement(['zip', 'individual']),
    totalCount: faker.number.int(),
    completedCount: faker.number.int(),
    errorCount: faker.number.int(),
    currentFile: faker.lorem.words(),
    errors: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    zipPath: faker.location.zipCode(),
    itemNames: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
  };
}

export function getBatchDownloadControllerGetUserTasksdefaultResponse() {
  return {
    tasks: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        taskId: faker.string.uuid(),
        status: faker.lorem.words(),
        mode: faker.helpers.arrayElement(['zip', 'individual']),
        totalCount: faker.number.int(),
        completedCount: faker.number.int(),
        errorCount: faker.number.int(),
        zipPath: faker.location.zipCode(),
        zipSize: faker.number.int(),
        itemNames: (() => {
          const arrayMin = 1;
          const arrayMax = MAX_ARRAY_LENGTH;
          const safeMin = Math.min(arrayMin, arrayMax);
          return [
            ...new Array(
              faker.number.int({ min: safeMin, max: arrayMax })
            ).keys(),
          ].map((_) => faker.lorem.words());
        })(),
      }));
    })(),
    hasMore: faker.datatype.boolean(),
  };
}

export function getBackupControllerTriggerBackup200Response() {
  return {
    success: true,
    filename: 'cloudcad-20260826-010000.dump',
    sizeBytes: faker.number.int(),
    durationMs: faker.number.int(),
    deletedCount: faker.number.int(),
  };
}

export function getBackupControllerListBackups200Response() {
  return {
    data: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        name: 'cloudcad-20260826-010000.dump',
        sizeBytes: 10485760,
        modifiedAt: faker.date.anytime().toISOString(),
      }));
    })(),
    total: 3,
  };
}

export function getBackupControllerDeleteBackup200Response() {
  return {
    success: true,
    name: faker.person.fullName(),
  };
}

export function getConversionMonitorControllerGetStats200Response() {
  return {
    mode: faker.helpers.arrayElement([
      'process-pool',
      'conversion-service',
      'cloud-faas',
    ]),
    processPool: {},
    conversionService: {},
    conversionServiceError: {},
    history: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => faker.lorem.words());
    })(),
    sampledAt: faker.number.int(),
  };
}

export function getConversionMonitorControllerListTasks200Response() {
  return {
    items: (() => {
      const arrayMin = 1;
      const arrayMax = MAX_ARRAY_LENGTH;
      const safeMin = Math.min(arrayMin, arrayMax);
      return [
        ...new Array(faker.number.int({ min: safeMin, max: arrayMax })).keys(),
      ].map((_) => ({
        id: faker.string.uuid(),
        type: faker.lorem.words(),
        status: faker.lorem.words(),
        progress: faker.number.int(),
        createdAt: faker.lorem.words(),
        updatedAt: faker.lorem.words(),
        startedAt: {},
        completedAt: {},
        error: faker.lorem.words(),
        contentKey: faker.lorem.words(),
      }));
    })(),
    total: faker.number.int(),
  };
}
