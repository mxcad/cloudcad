# CloudCAD 功能对齐 — 执行计划

## 背景
- main 分支：用户可见行为完全正确，但代码质量差（as any、硬编码）
- 当前分支：类型安全、模块化，但功能缺失、交互残缺、样式漏洞

## 目标
让当前分支的行为逻辑、用户交互、页面样式与 main 保持一致，同时保留当前分支更好的实现方式。

## 约束
- 不改 package.json、.env、CI 配置、数据库迁移
- 不删文件
- 不 push
- 不把代码降级成 main 的写法

## 产出物
1. 功能差异清单
2. 按优先级排序的原子任务列表

现在开始，先扫描项目，生成完整的功能差异清单和任务拆分。